# [PROJECT NAME] Development Guidelines

Last updated: [DATE]

## Project Context
[Brief summary of the project's purpose and architecture]

## Core Technologies
- **Language**: [e.g. TypeScript, Python]
- **Framework**: [e.g. React, FastAPI]
- **Database**: [e.g. PostgreSQL, Redis]

## Project Structure
```
src/
├── components/   # UI components
├── services/     # Business logic
├── models/       # Data definitions
└── utils/        # Shared helpers
```

## Naming Conventions
- **Files**: hyphen-case (e.g. `user-profile.tsx`)
- **Variables**: camelCase (e.g. `isUserActive`)
- **Classes**: PascalCase (e.g. `UserService`)

## Code Style
- Use [Tool] for formatting (e.g. Prettier)
- [Rule 1]: e.g. Always use functional components
- [Rule 2]: e.g. Prefer async/await over promises

## Testing Standards
- All new features require unit tests
- Minimum coverage: 80%
- Run tests with `[Command]`

## Verification Steps
1. Run linting: `[Command]`
2. Run build: `[Command]`
3. Run tests: `[Command]`
