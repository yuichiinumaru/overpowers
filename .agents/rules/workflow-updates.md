---
trigger: always_on
---

Sempre que fizer updates em workflows/commands ou criar novos commands neste projeto, rode o script de conversão para toml para que os scripts fiquem disponíveis para o Gemini-CLI (que é uma exceção à regra e usa TOML em vez de markdown).