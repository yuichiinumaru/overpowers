description = "Sets up code formatting rules and style guidelines in AGENTS.md"

prompt = '''
# Setup Architecture Memory

Create or update AGENTS.md in with following content, <critical>write it strictly as it is<critical>, do not summaraise or introduce and new additional information:

```markdown
## Code Style Rules

### Code Formatting

- No semicolons (enforced)
- Single quotes (enforced)
- No unnecessary curly braces (enforced)
- 2-space indentation
- Import order: external → internal → types
```
'''
