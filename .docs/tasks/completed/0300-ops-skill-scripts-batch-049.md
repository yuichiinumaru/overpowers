# Task 0300: Skill Scripts Batch 049

**Status**: [x]
**Priority**: LOW
**Type**: ops

## Objective
Analyze each skill in this batch and create helper scripts inside their `scripts/` subdirectory where it makes sense, based on the `SKILL.md` instructions.

## Sub-tasks
- [x] `sec-safety-1012-sec-safety-0554-incident-runbook-templates`: Check if any existing script inside the repository's `scripts/` subfolders would fit as a helper and copy it over (if applicable)
- [x] `sec-safety-1012-sec-safety-0554-incident-runbook-templates`: Analyze and create new helper scripts inside its `scripts/` directory (if applicable)
- [x] `sec-safety-1013-sec-safety-0555-indirect-prompt-injection`: Check if any existing script inside the repository's `scripts/` subfolders would fit as a helper and copy it over (if applicable)
- [x] `sec-safety-1013-sec-safety-0555-indirect-prompt-injection`: Analyze and create new helper scripts inside its `scripts/` directory (if applicable)
- [x] `sec-safety-1014-sec-safety-0572-ios-developer`: Check if any existing script inside the repository's `scripts/` subfolders would fit as a helper and copy it over (if applicable)
- [x] `sec-safety-1014-sec-safety-0572-ios-developer`: Analyze and create new helper scripts inside its `scripts/` directory (if applicable)
- [x] `sec-safety-1015-sec-safety-0577-istio-traffic-management`: Check if any existing script inside the repository's `scripts/` subfolders would fit as a helper and copy it over (if applicable)
- [x] `sec-safety-1015-sec-safety-0577-istio-traffic-management`: Analyze and create new helper scripts inside its `scripts/` directory (if applicable)
- [x] `sec-safety-1016-sec-safety-0578-it-operations`: Check if any existing script inside the repository's `scripts/` subfolders would fit as a helper and copy it over (if applicable)
- [x] `sec-safety-1016-sec-safety-0578-it-operations`: Analyze and create new helper scripts inside its `scripts/` directory (if applicable)
- [x] `sec-safety-1017-sec-safety-0580-jiang-irac-opposition`: Check if any existing script inside the repository's `scripts/` subfolders would fit as a helper and copy it over (if applicable)
- [x] `sec-safety-1017-sec-safety-0580-jiang-irac-opposition`: Analyze and create new helper scripts inside its `scripts/` directory (if applicable)
- [x] `sec-safety-1018-sec-safety-0584-jira-issues`: Check if any existing script inside the repository's `scripts/` subfolders would fit as a helper and copy it over (if applicable)
- [x] `sec-safety-1018-sec-safety-0584-jira-issues`: Analyze and create new helper scripts inside its `scripts/` directory (if applicable)
- [x] `sec-safety-1019-sec-safety-0586-jules-dispatch`: Check if any existing script inside the repository's `scripts/` subfolders would fit as a helper and copy it over (if applicable)
- [x] `sec-safety-1019-sec-safety-0586-jules-dispatch`: Analyze and create new helper scripts inside its `scripts/` directory (if applicable)
- [x] `sec-safety-1020-sec-safety-0589-jules-integrate`: Check if any existing script inside the repository's `scripts/` subfolders would fit as a helper and copy it over (if applicable)
- [x] `sec-safety-1020-sec-safety-0589-jules-integrate`: Analyze and create new helper scripts inside its `scripts/` directory (if applicable)
- [x] `sec-safety-1021-sec-safety-0590-jules-triage`: Check if any existing script inside the repository's `scripts/` subfolders would fit as a helper and copy it over (if applicable)
- [x] `sec-safety-1021-sec-safety-0590-jules-triage`: Analyze and create new helper scripts inside its `scripts/` directory (if applicable)
- [x] `sec-safety-1022-sec-safety-0591-k8s-manifest-generator`: Check if any existing script inside the repository's `scripts/` subfolders would fit as a helper and copy it over (if applicable)
- [x] `sec-safety-1022-sec-safety-0591-k8s-manifest-generator`: Analyze and create new helper scripts inside its `scripts/` directory (if applicable)
- [x] `sec-safety-1023-sec-safety-0592-k8s-security-policies`: Check if any existing script inside the repository's `scripts/` subfolders would fit as a helper and copy it over (if applicable)
- [x] `sec-safety-1023-sec-safety-0592-k8s-security-policies`: Analyze and create new helper scripts inside its `scripts/` directory (if applicable)
- [x] `sec-safety-1024-sec-safety-0599-kreuzberg`: Check if any existing script inside the repository's `scripts/` subfolders would fit as a helper and copy it over (if applicable)
- [x] `sec-safety-1024-sec-safety-0599-kreuzberg`: Analyze and create new helper scripts inside its `scripts/` directory (if applicable)
- [x] `sec-safety-1025-sec-safety-0600-labarchive-integration`: Check if any existing script inside the repository's `scripts/` subfolders would fit as a helper and copy it over (if applicable)
- [x] `sec-safety-1025-sec-safety-0600-labarchive-integration`: Analyze and create new helper scripts inside its `scripts/` directory (if applicable)
- [x] `sec-safety-1026-sec-safety-0601-lamindb`: Check if any existing script inside the repository's `scripts/` subfolders would fit as a helper and copy it over (if applicable)
- [x] `sec-safety-1026-sec-safety-0601-lamindb`: Analyze and create new helper scripts inside its `scripts/` directory (if applicable)
- [x] `sec-safety-1027-sec-safety-0611-linear-claude-skill`: Check if any existing script inside the repository's `scripts/` subfolders would fit as a helper and copy it over (if applicable)
- [x] `sec-safety-1027-sec-safety-0611-linear-claude-skill`: Analyze and create new helper scripts inside its `scripts/` directory (if applicable)
- [x] `sec-safety-1028-sec-safety-0614-linkerd-patterns`: Check if any existing script inside the repository's `scripts/` subfolders would fit as a helper and copy it over (if applicable)
- [x] `sec-safety-1028-sec-safety-0614-linkerd-patterns`: Analyze and create new helper scripts inside its `scripts/` directory (if applicable)
- [x] `sec-safety-1029-sec-safety-0615-linux-privilege-escalation`: Check if any existing script inside the repository's `scripts/` subfolders would fit as a helper and copy it over (if applicable)
- [x] `sec-safety-1029-sec-safety-0615-linux-privilege-escalation`: Analyze and create new helper scripts inside its `scripts/` directory (if applicable)
- [x] `sec-safety-1030-sec-safety-0620-llm-security`: Check if any existing script inside the repository's `scripts/` subfolders would fit as a helper and copy it over (if applicable)
- [x] `sec-safety-1030-sec-safety-0620-llm-security`: Analyze and create new helper scripts inside its `scripts/` directory (if applicable)
- [x] `sec-safety-1031-sec-safety-0629-manipulation-detector`: Check if any existing script inside the repository's `scripts/` subfolders would fit as a helper and copy it over (if applicable)
- [x] `sec-safety-1031-sec-safety-0629-manipulation-detector`: Analyze and create new helper scripts inside its `scripts/` directory (if applicable)
