# Task 0300: Skill Scripts Batch 035

**Status**: [x]
**Priority**: LOW
**Type**: ops

## Objective
Analyze each skill in this batch and create helper scripts inside their `scripts/` subdirectory where it makes sense, based on the `SKILL.md` instructions.

## Sub-tasks
- [x] `ops-infra-0714-ops-infra-1092-test-fixing`: Check if any existing script inside the repository's `scripts/` subfolders would fit as a helper and copy it over (if applicable)
- [x] `ops-infra-0714-ops-infra-1092-test-fixing`: Analyze and create new helper scripts inside its `scripts/` directory (if applicable)
- [x] `ops-infra-0715-ops-infra-1095-things-mac`: Check if any existing script inside the repository's `scripts/` subfolders would fit as a helper and copy it over (if applicable)
- [x] `ops-infra-0715-ops-infra-1095-things-mac`: Analyze and create new helper scripts inside its `scripts/` directory (if applicable)
- [x] `ops-infra-0716-ops-infra-1100-threejs-geometry`: Check if any existing script inside the repository's `scripts/` subfolders would fit as a helper and copy it over (if applicable)
- [x] `ops-infra-0716-ops-infra-1100-threejs-geometry`: Analyze and create new helper scripts inside its `scripts/` directory (if applicable)
- [x] `ops-infra-0717-ops-infra-1112-todoist-automation`: Check if any existing script inside the repository's `scripts/` subfolders would fit as a helper and copy it over (if applicable)
- [x] `ops-infra-0717-ops-infra-1112-todoist-automation`: Analyze and create new helper scripts inside its `scripts/` directory (if applicable)
- [x] `ops-infra-0718-ops-infra-1139-trello`: Check if any existing script inside the repository's `scripts/` subfolders would fit as a helper and copy it over (if applicable)
- [x] `ops-infra-0718-ops-infra-1139-trello`: Analyze and create new helper scripts inside its `scripts/` directory (if applicable)
- [x] `ops-infra-0719-ops-infra-1140-trello-automation`: Check if any existing script inside the repository's `scripts/` subfolders would fit as a helper and copy it over (if applicable)
- [x] `ops-infra-0719-ops-infra-1140-trello-automation`: Analyze and create new helper scripts inside its `scripts/` directory (if applicable)
- [x] `ops-infra-0720-ops-infra-1154-update-docs`: Check if any existing script inside the repository's `scripts/` subfolders would fit as a helper and copy it over (if applicable)
- [x] `ops-infra-0720-ops-infra-1154-update-docs`: Analyze and create new helper scripts inside its `scripts/` directory (if applicable)
- [x] `ops-infra-0721-ops-infra-1155-upgrading-expo`: Check if any existing script inside the repository's `scripts/` subfolders would fit as a helper and copy it over (if applicable)
- [x] `ops-infra-0721-ops-infra-1155-upgrading-expo`: Analyze and create new helper scripts inside its `scripts/` directory (if applicable)
- [x] `ops-infra-0722-ops-infra-1157-user-file-ops`: Check if any existing script inside the repository's `scripts/` subfolders would fit as a helper and copy it over (if applicable)
- [x] `ops-infra-0722-ops-infra-1157-user-file-ops`: Analyze and create new helper scripts inside its `scripts/` directory (if applicable)
- [x] `ops-infra-0723-ops-infra-1158-using-git-worktrees`: Check if any existing script inside the repository's `scripts/` subfolders would fit as a helper and copy it over (if applicable)
- [x] `ops-infra-0723-ops-infra-1158-using-git-worktrees`: Analyze and create new helper scripts inside its `scripts/` directory (if applicable)
- [x] `ops-infra-0724-ops-infra-1202-web-fetch`: Check if any existing script inside the repository's `scripts/` subfolders would fit as a helper and copy it over (if applicable)
- [x] `ops-infra-0724-ops-infra-1202-web-fetch`: Analyze and create new helper scripts inside its `scripts/` directory (if applicable)
- [x] `ops-infra-0725-ops-infra-1207-web-to-markdown`: Check if any existing script inside the repository's `scripts/` subfolders would fit as a helper and copy it over (if applicable)
- [x] `ops-infra-0725-ops-infra-1207-web-to-markdown`: Analyze and create new helper scripts inside its `scripts/` directory (if applicable)
- [x] `ops-infra-0726-ops-infra-1209-webflow-automation`: Check if any existing script inside the repository's `scripts/` subfolders would fit as a helper and copy it over (if applicable)
- [x] `ops-infra-0726-ops-infra-1209-webflow-automation`: Analyze and create new helper scripts inside its `scripts/` directory (if applicable)
- [x] `ops-infra-0727-ops-infra-1212-whatsapp-automation`: Check if any existing script inside the repository's `scripts/` subfolders would fit as a helper and copy it over (if applicable)
- [x] `ops-infra-0727-ops-infra-1212-whatsapp-automation`: Analyze and create new helper scripts inside its `scripts/` directory (if applicable)
- [x] `ops-infra-0728-ops-infra-1219-workflow-patterns`: Check if any existing script inside the repository's `scripts/` subfolders would fit as a helper and copy it over (if applicable)
- [x] `ops-infra-0728-ops-infra-1219-workflow-patterns`: Analyze and create new helper scripts inside its `scripts/` directory (if applicable)
- [x] `ops-infra-0729-ops-infra-1223-wrike-automation`: Check if any existing script inside the repository's `scripts/` subfolders would fit as a helper and copy it over (if applicable)
- [x] `ops-infra-0729-ops-infra-1223-wrike-automation`: Analyze and create new helper scripts inside its `scripts/` directory (if applicable)
- [x] `ops-infra-0730-ops-infra-1229-x-article-publisher-skill`: Check if any existing script inside the repository's `scripts/` subfolders would fit as a helper and copy it over (if applicable)
- [x] `ops-infra-0730-ops-infra-1229-x-article-publisher-skill`: Analyze and create new helper scripts inside its `scripts/` directory (if applicable)
- [x] `ops-infra-0731-ops-infra-1244-zoho-crm-automation`: Check if any existing script inside the repository's `scripts/` subfolders would fit as a helper and copy it over (if applicable)
- [x] `ops-infra-0731-ops-infra-1244-zoho-crm-automation`: Analyze and create new helper scripts inside its `scripts/` directory (if applicable)
- [x] `ops-infra-0732-ops-infra-1245-zoom-automation`: Check if any existing script inside the repository's `scripts/` subfolders would fit as a helper and copy it over (if applicable)
- [x] `ops-infra-0732-ops-infra-1245-zoom-automation`: Analyze and create new helper scripts inside its `scripts/` directory (if applicable)
- [x] `ops-infra-0733-cloudflare-deploy`: Check if any existing script inside the repository's `scripts/` subfolders would fit as a helper and copy it over (if applicable)
- [x] `ops-infra-0733-cloudflare-deploy`: Analyze and create new helper scripts inside its `scripts/` directory (if applicable)
