# Task 0300: Skill Scripts Batch 034

**Status**: [x]
**Priority**: LOW
**Type**: ops

## Objective
Analyze each skill in this batch and create helper scripts inside their `scripts/` subdirectory where it makes sense, based on the `SKILL.md` instructions.

## Sub-tasks
- [x] `ops-infra-0693-ops-infra-0896-react-state-management`: Check if any existing script inside the repository's `scripts/` subfolders would fit as a helper and copy it over (if applicable)
- [x] `ops-infra-0693-ops-infra-0896-react-state-management`: Analyze and create new helper scripts inside its `scripts/` directory (if applicable)
- [x] `ops-infra-0694-ops-infra-0910-regulatory-affairs-head`: Check if any existing script inside the repository's `scripts/` subfolders would fit as a helper and copy it over (if applicable)
- [x] `ops-infra-0694-ops-infra-0910-regulatory-affairs-head`: Analyze and create new helper scripts inside its `scripts/` directory (if applicable)
- [x] `ops-infra-0696-ops-infra-0935-risk-metrics-calculation`: Check if any existing script inside the repository's `scripts/` subfolders would fit as a helper and copy it over (if applicable)
- [x] `ops-infra-0696-ops-infra-0935-risk-metrics-calculation`: Analyze and create new helper scripts inside its `scripts/` directory (if applicable)
- [x] `ops-infra-0697-ops-infra-0941-salesforce-automation`: Check if any existing script inside the repository's `scripts/` subfolders would fit as a helper and copy it over (if applicable)
- [x] `ops-infra-0697-ops-infra-0941-salesforce-automation`: Analyze and create new helper scripts inside its `scripts/` directory (if applicable)
- [x] `ops-infra-0698-ops-infra-0947-schema-markup`: Check if any existing script inside the repository's `scripts/` subfolders would fit as a helper and copy it over (if applicable)
- [x] `ops-infra-0698-ops-infra-0947-schema-markup`: Analyze and create new helper scripts inside its `scripts/` directory (if applicable)
- [x] `ops-infra-0699-ops-infra-0962-search-strategy`: Check if any existing script inside the repository's `scripts/` subfolders would fit as a helper and copy it over (if applicable)
- [x] `ops-infra-0699-ops-infra-0962-search-strategy`: Analyze and create new helper scripts inside its `scripts/` directory (if applicable)
- [x] `ops-infra-0700-ops-infra-0983-seo-cannibalization-detector`: Check if any existing script inside the repository's `scripts/` subfolders would fit as a helper and copy it over (if applicable)
- [x] `ops-infra-0700-ops-infra-0983-seo-cannibalization-detector`: Analyze and create new helper scripts inside its `scripts/` directory (if applicable)
- [x] `ops-infra-0701-ops-infra-0998-service-mesh-observability`: Check if any existing script inside the repository's `scripts/` subfolders would fit as a helper and copy it over (if applicable)
- [x] `ops-infra-0701-ops-infra-0998-service-mesh-observability`: Analyze and create new helper scripts inside its `scripts/` directory (if applicable)
- [x] `ops-infra-0702-ops-infra-1001-sheets-cli`: Check if any existing script inside the repository's `scripts/` subfolders would fit as a helper and copy it over (if applicable)
- [x] `ops-infra-0702-ops-infra-1001-sheets-cli`: Analyze and create new helper scripts inside its `scripts/` directory (if applicable)
- [x] `ops-infra-0703-ops-infra-1002-sherpa-onnx-tts`: Check if any existing script inside the repository's `scripts/` subfolders would fit as a helper and copy it over (if applicable)
- [x] `ops-infra-0703-ops-infra-1002-sherpa-onnx-tts`: Analyze and create new helper scripts inside its `scripts/` directory (if applicable)
- [x] `ops-infra-0704-ops-infra-1003-shopify-automation`: Check if any existing script inside the repository's `scripts/` subfolders would fit as a helper and copy it over (if applicable)
- [x] `ops-infra-0704-ops-infra-1003-shopify-automation`: Analyze and create new helper scripts inside its `scripts/` directory (if applicable)
- [x] `ops-infra-0705-ops-infra-1029-slash-command-factory`: Check if any existing script inside the repository's `scripts/` subfolders would fit as a helper and copy it over (if applicable)
- [x] `ops-infra-0705-ops-infra-1029-slash-command-factory`: Analyze and create new helper scripts inside its `scripts/` directory (if applicable)
- [x] `ops-infra-0706-ops-infra-1031-snapdom`: Check if any existing script inside the repository's `scripts/` subfolders would fit as a helper and copy it over (if applicable)
- [x] `ops-infra-0706-ops-infra-1031-snapdom`: Analyze and create new helper scripts inside its `scripts/` directory (if applicable)
- [x] `ops-infra-0707-ops-infra-1033-social-media`: Check if any existing script inside the repository's `scripts/` subfolders would fit as a helper and copy it over (if applicable)
- [x] `ops-infra-0707-ops-infra-1033-social-media`: Analyze and create new helper scripts inside its `scripts/` directory (if applicable)
- [x] `ops-infra-0708-ops-infra-1045-square-automation`: Check if any existing script inside the repository's `scripts/` subfolders would fit as a helper and copy it over (if applicable)
- [x] `ops-infra-0708-ops-infra-1045-square-automation`: Analyze and create new helper scripts inside its `scripts/` directory (if applicable)
- [x] `ops-infra-0709-ops-infra-1060-stripe-automation`: Check if any existing script inside the repository's `scripts/` subfolders would fit as a helper and copy it over (if applicable)
- [x] `ops-infra-0709-ops-infra-1060-stripe-automation`: Analyze and create new helper scripts inside its `scripts/` directory (if applicable)
- [x] `ops-infra-0710-ops-infra-1065-summarize-activity`: Check if any existing script inside the repository's `scripts/` subfolders would fit as a helper and copy it over (if applicable)
- [x] `ops-infra-0710-ops-infra-1065-summarize-activity`: Analyze and create new helper scripts inside its `scripts/` directory (if applicable)
- [x] `ops-infra-0711-ops-infra-1082-team-composition-analysis`: Check if any existing script inside the repository's `scripts/` subfolders would fit as a helper and copy it over (if applicable)
- [x] `ops-infra-0711-ops-infra-1082-team-composition-analysis`: Analyze and create new helper scripts inside its `scripts/` directory (if applicable)
- [x] `ops-infra-0712-ops-infra-1086-technical-writer`: Check if any existing script inside the repository's `scripts/` subfolders would fit as a helper and copy it over (if applicable)
- [x] `ops-infra-0712-ops-infra-1086-technical-writer`: Analyze and create new helper scripts inside its `scripts/` directory (if applicable)
- [x] `ops-infra-0713-ops-infra-1088-terraform-module-library`: Check if any existing script inside the repository's `scripts/` subfolders would fit as a helper and copy it over (if applicable)
- [x] `ops-infra-0713-ops-infra-1088-terraform-module-library`: Analyze and create new helper scripts inside its `scripts/` directory (if applicable)
