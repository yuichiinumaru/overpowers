# Task 0300: Skill Scripts Batch 031

**Status**: [x]
**Priority**: LOW
**Type**: ops

## Objective
Analyze each skill in this batch and create helper scripts inside their `scripts/` subdirectory where it makes sense, based on the `SKILL.md` instructions.

## Sub-tasks
- [x] `ops-infra-0628-ops-infra-0478-gh-fix-ci`: Check if any existing script inside the repository's `scripts/` subfolders would fit as a helper and copy it over (if applicable)
- [x] `ops-infra-0628-ops-infra-0478-gh-fix-ci`: Analyze and create new helper scripts inside its `scripts/` directory (if applicable)
- [x] `ops-infra-0631-ops-infra-0489-github-issues`: Check if any existing script inside the repository's `scripts/` subfolders would fit as a helper and copy it over (if applicable)
- [x] `ops-infra-0631-ops-infra-0489-github-issues`: Analyze and create new helper scripts inside its `scripts/` directory (if applicable)
- [x] `ops-infra-0632-ops-infra-0495-gitlab-automation`: Check if any existing script inside the repository's `scripts/` subfolders would fit as a helper and copy it over (if applicable)
- [x] `ops-infra-0632-ops-infra-0495-gitlab-automation`: Analyze and create new helper scripts inside its `scripts/` directory (if applicable)
- [x] `ops-infra-0633-ops-infra-0500-google-analytics-automation`: Check if any existing script inside the repository's `scripts/` subfolders would fit as a helper and copy it over (if applicable)
- [x] `ops-infra-0633-ops-infra-0500-google-analytics-automation`: Analyze and create new helper scripts inside its `scripts/` directory (if applicable)
- [x] `ops-infra-0634-ops-infra-0501-google-calendar-automation`: Check if any existing script inside the repository's `scripts/` subfolders would fit as a helper and copy it over (if applicable)
- [x] `ops-infra-0634-ops-infra-0501-google-calendar-automation`: Analyze and create new helper scripts inside its `scripts/` directory (if applicable)
- [x] `ops-infra-0635-ops-infra-0502-google-workspace`: Check if any existing script inside the repository's `scripts/` subfolders would fit as a helper and copy it over (if applicable)
- [x] `ops-infra-0635-ops-infra-0502-google-workspace`: Analyze and create new helper scripts inside its `scripts/` directory (if applicable)
- [x] `ops-infra-0636-ops-infra-0503-googlesheets-automation`: Check if any existing script inside the repository's `scripts/` subfolders would fit as a helper and copy it over (if applicable)
- [x] `ops-infra-0636-ops-infra-0503-googlesheets-automation`: Analyze and create new helper scripts inside its `scripts/` directory (if applicable)
- [x] `ops-infra-0637-ops-infra-0506-grafana-dashboards`: Check if any existing script inside the repository's `scripts/` subfolders would fit as a helper and copy it over (if applicable)
- [x] `ops-infra-0637-ops-infra-0506-grafana-dashboards`: Analyze and create new helper scripts inside its `scripts/` directory (if applicable)
- [x] `ops-infra-0640-ops-infra-0532-html2pptx`: Check if any existing script inside the repository's `scripts/` subfolders would fit as a helper and copy it over (if applicable)
- [x] `ops-infra-0640-ops-infra-0532-html2pptx`: Analyze and create new helper scripts inside its `scripts/` directory (if applicable)
- [x] `ops-infra-0641-ops-infra-0533-hubspot-automation`: Check if any existing script inside the repository's `scripts/` subfolders would fit as a helper and copy it over (if applicable)
- [x] `ops-infra-0641-ops-infra-0533-hubspot-automation`: Analyze and create new helper scripts inside its `scripts/` directory (if applicable)
- [x] `ops-infra-0642-ops-infra-0552-imsg`: Check if any existing script inside the repository's `scripts/` subfolders would fit as a helper and copy it over (if applicable)
- [x] `ops-infra-0642-ops-infra-0552-imsg`: Analyze and create new helper scripts inside its `scripts/` directory (if applicable)
- [x] `ops-infra-0643-ops-infra-0557-infographic-creator`: Check if any existing script inside the repository's `scripts/` subfolders would fit as a helper and copy it over (if applicable)
- [x] `ops-infra-0643-ops-infra-0557-infographic-creator`: Analyze and create new helper scripts inside its `scripts/` directory (if applicable)
- [x] `ops-infra-0644-ops-infra-0566-interactive-portfolio`: Check if any existing script inside the repository's `scripts/` subfolders would fit as a helper and copy it over (if applicable)
- [x] `ops-infra-0644-ops-infra-0566-interactive-portfolio`: Analyze and create new helper scripts inside its `scripts/` directory (if applicable)
- [x] `ops-infra-0645-ops-infra-0567-intercom-automation`: Check if any existing script inside the repository's `scripts/` subfolders would fit as a helper and copy it over (if applicable)
- [x] `ops-infra-0645-ops-infra-0567-intercom-automation`: Analyze and create new helper scripts inside its `scripts/` directory (if applicable)
- [x] `ops-infra-0646-ops-infra-0568-internal-comms`: Check if any existing script inside the repository's `scripts/` subfolders would fit as a helper and copy it over (if applicable)
- [x] `ops-infra-0646-ops-infra-0568-internal-comms`: Analyze and create new helper scripts inside its `scripts/` directory (if applicable)
- [x] `ops-infra-0647-ops-infra-0576-issue-triage`: Check if any existing script inside the repository's `scripts/` subfolders would fit as a helper and copy it over (if applicable)
- [x] `ops-infra-0647-ops-infra-0576-issue-triage`: Analyze and create new helper scripts inside its `scripts/` directory (if applicable)
- [x] `ops-infra-0648-ops-infra-0583-jira-automation`: Check if any existing script inside the repository's `scripts/` subfolders would fit as a helper and copy it over (if applicable)
- [x] `ops-infra-0648-ops-infra-0583-jira-automation`: Analyze and create new helper scripts inside its `scripts/` directory (if applicable)
- [x] `ops-infra-0649-ops-infra-0595-klaviyo-automation`: Check if any existing script inside the repository's `scripts/` subfolders would fit as a helper and copy it over (if applicable)
- [x] `ops-infra-0649-ops-infra-0595-klaviyo-automation`: Analyze and create new helper scripts inside its `scripts/` directory (if applicable)
- [x] `ops-infra-0650-ops-infra-0597-knowledge-synthesis`: Check if any existing script inside the repository's `scripts/` subfolders would fit as a helper and copy it over (if applicable)
- [x] `ops-infra-0650-ops-infra-0597-knowledge-synthesis`: Analyze and create new helper scripts inside its `scripts/` directory (if applicable)
- [x] `ops-infra-0651-ops-infra-0598-kpi-dashboard-design`: Check if any existing script inside the repository's `scripts/` subfolders would fit as a helper and copy it over (if applicable)
- [x] `ops-infra-0651-ops-infra-0598-kpi-dashboard-design`: Analyze and create new helper scripts inside its `scripts/` directory (if applicable)
