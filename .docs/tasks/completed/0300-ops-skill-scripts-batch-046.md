# Task 0300: Skill Scripts Batch 046

**Status**: [x]
**Priority**: LOW
**Type**: ops

## Objective
Analyze each skill in this batch and create helper scripts inside their `scripts/` subdirectory where it makes sense, based on the `SKILL.md` instructions.

## Sub-tasks
- [x] `sec-safety-0947-sec-safety-0192-cc-skill-project-guidelines-example`: Check if any existing script inside the repository's `scripts/` subfolders would fit as a helper and copy it over (if applicable)
- [x] `sec-safety-0947-sec-safety-0192-cc-skill-project-guidelines-example`: Analyze and create new helper scripts inside its `scripts/` directory (if applicable)
- [x] `sec-safety-0948-sec-safety-0197-changelog-generator`: Check if any existing script inside the repository's `scripts/` subfolders would fit as a helper and copy it over (if applicable)
- [x] `sec-safety-0948-sec-safety-0197-changelog-generator`: Analyze and create new helper scripts inside its `scripts/` directory (if applicable)
- [x] `sec-safety-0949-sec-safety-0216-clinical-reports`: Check if any existing script inside the repository's `scripts/` subfolders would fit as a helper and copy it over (if applicable)
- [x] `sec-safety-0949-sec-safety-0216-clinical-reports`: Analyze and create new helper scripts inside its `scripts/` directory (if applicable)
- [x] `sec-safety-0950-sec-safety-0221-cloud-architect`: Check if any existing script inside the repository's `scripts/` subfolders would fit as a helper and copy it over (if applicable)
- [x] `sec-safety-0950-sec-safety-0221-cloud-architect`: Analyze and create new helper scripts inside its `scripts/` directory (if applicable)
- [x] `sec-safety-0951-sec-safety-0222-cloud-penetration-testing`: Check if any existing script inside the repository's `scripts/` subfolders would fit as a helper and copy it over (if applicable)
- [x] `sec-safety-0951-sec-safety-0222-cloud-penetration-testing`: Analyze and create new helper scripts inside its `scripts/` directory (if applicable)
- [x] `sec-safety-0952-sec-safety-0223-cloudflare`: Check if any existing script inside the repository's `scripts/` subfolders would fit as a helper and copy it over (if applicable)
- [x] `sec-safety-0952-sec-safety-0223-cloudflare`: Analyze and create new helper scripts inside its `scripts/` directory (if applicable)
- [x] `sec-safety-0954-sec-safety-0231-code-review`: Check if any existing script inside the repository's `scripts/` subfolders would fit as a helper and copy it over (if applicable)
- [x] `sec-safety-0954-sec-safety-0231-code-review`: Analyze and create new helper scripts inside its `scripts/` directory (if applicable)
- [x] `sec-safety-0955-sec-safety-0232-code-reviewer`: Check if any existing script inside the repository's `scripts/` subfolders would fit as a helper and copy it over (if applicable)
- [x] `sec-safety-0955-sec-safety-0232-code-reviewer`: Analyze and create new helper scripts inside its `scripts/` directory (if applicable)
- [x] `sec-safety-0956-sec-safety-0233-code-security`: Check if any existing script inside the repository's `scripts/` subfolders would fit as a helper and copy it over (if applicable)
- [x] `sec-safety-0956-sec-safety-0233-code-security`: Analyze and create new helper scripts inside its `scripts/` directory (if applicable)
- [x] `sec-safety-0957-sec-safety-0235-codebase-documenter`: Check if any existing script inside the repository's `scripts/` subfolders would fit as a helper and copy it over (if applicable)
- [x] `sec-safety-0957-sec-safety-0235-codebase-documenter`: Analyze and create new helper scripts inside its `scripts/` directory (if applicable)
- [x] `sec-safety-0958-sec-safety-0244-competitive-ads-extractor`: Check if any existing script inside the repository's `scripts/` subfolders would fit as a helper and copy it over (if applicable)
- [x] `sec-safety-0958-sec-safety-0244-competitive-ads-extractor`: Analyze and create new helper scripts inside its `scripts/` directory (if applicable)
- [x] `sec-safety-0959-sec-safety-0252-comprehensive-review-full-review`: Check if any existing script inside the repository's `scripts/` subfolders would fit as a helper and copy it over (if applicable)
- [x] `sec-safety-0959-sec-safety-0252-comprehensive-review-full-review`: Analyze and create new helper scripts inside its `scripts/` directory (if applicable)
- [x] `sec-safety-0960-sec-safety-0269-contract-review`: Check if any existing script inside the repository's `scripts/` subfolders would fit as a helper and copy it over (if applicable)
- [x] `sec-safety-0960-sec-safety-0269-contract-review`: Analyze and create new helper scripts inside its `scripts/` directory (if applicable)
- [x] `sec-safety-0961-sec-safety-0277-council`: Check if any existing script inside the repository's `scripts/` subfolders would fit as a helper and copy it over (if applicable)
- [x] `sec-safety-0961-sec-safety-0277-council`: Analyze and create new helper scripts inside its `scripts/` directory (if applicable)
- [x] `sec-safety-0962-sec-safety-0278-create-an-asset`: Check if any existing script inside the repository's `scripts/` subfolders would fit as a helper and copy it over (if applicable)
- [x] `sec-safety-0962-sec-safety-0278-create-an-asset`: Analyze and create new helper scripts inside its `scripts/` directory (if applicable)
- [x] `sec-safety-0963-sec-safety-0282-create-pattern`: Check if any existing script inside the repository's `scripts/` subfolders would fit as a helper and copy it over (if applicable)
- [x] `sec-safety-0963-sec-safety-0282-create-pattern`: Analyze and create new helper scripts inside its `scripts/` directory (if applicable)
- [x] `sec-safety-0965-sec-safety-0290-cto-advisor`: Check if any existing script inside the repository's `scripts/` subfolders would fit as a helper and copy it over (if applicable)
- [x] `sec-safety-0965-sec-safety-0290-cto-advisor`: Analyze and create new helper scripts inside its `scripts/` directory (if applicable)
- [x] `sec-safety-0966-sec-safety-0291-customer-research`: Check if any existing script inside the repository's `scripts/` subfolders would fit as a helper and copy it over (if applicable)
- [x] `sec-safety-0966-sec-safety-0291-customer-research`: Analyze and create new helper scripts inside its `scripts/` directory (if applicable)
- [x] `sec-safety-0967-sec-safety-0292-customer-support`: Check if any existing script inside the repository's `scripts/` subfolders would fit as a helper and copy it over (if applicable)
- [x] `sec-safety-0967-sec-safety-0292-customer-support`: Analyze and create new helper scripts inside its `scripts/` directory (if applicable)
- [x] `sec-safety-0968-sec-safety-0296-daily.dev`: Check if any existing script inside the repository's `scripts/` subfolders would fit as a helper and copy it over (if applicable)
- [x] `sec-safety-0968-sec-safety-0296-daily.dev`: Analyze and create new helper scripts inside its `scripts/` directory (if applicable)
