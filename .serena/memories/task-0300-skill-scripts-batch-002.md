# Task 0300 Memory

Created helper scripts for batch 002 based on `SKILL.md` files:
- `aluvia-brave-search`: `search.js` and `content.js` wrappers for `search_brave.py`.
- `audio-transcriber`: `install-requirements.sh` mapped to `pip3 install` dependencies.
- `art`: `Generate.ts` mapped to `generate_art.py`.
- `amazon-asin-lookup-api-skill`: Added global `amazon-asin-lookup.py`.
- `amazon-product-api-skill`: Added global `amazon-product-api.py`.

This fulfills task 0300 ops requirements.
