# Task 0300: Skill Scripts Batch 044

**Status**: [x]
**Priority**: LOW
**Type**: ops

## Objective
Analyze each skill in this batch and create helper scripts inside their `scripts/` subdirectory where it makes sense, based on the `SKILL.md` instructions.

## Sub-tasks
[x] `sci-sci-0901-sci-sci-1007-simpy`: Check if any existing script inside the repository's `scripts/` subfolders would fit as a helper and copy it over (if applicable)
[x] `sci-sci-0901-sci-sci-1007-simpy`: Analyze and create new helper scripts inside its `scripts/` directory (if applicable)
[x] `sci-sci-0904-sci-sci-1071-tailored-resume-generator`: Check if any existing script inside the repository's `scripts/` subfolders would fit as a helper and copy it over (if applicable)
[x] `sci-sci-0904-sci-sci-1071-tailored-resume-generator`: Analyze and create new helper scripts inside its `scripts/` directory (if applicable)
[x] `sci-sci-0905-sci-sci-1178-verify-claims`: Check if any existing script inside the repository's `scripts/` subfolders would fit as a helper and copy it over (if applicable)
[x] `sci-sci-0905-sci-sci-1178-verify-claims`: Analyze and create new helper scripts inside its `scripts/` directory (if applicable)
[x] `sec-safety-0907-sec-safety-0002-1password`: Check if any existing script inside the repository's `scripts/` subfolders would fit as a helper and copy it over (if applicable)
[x] `sec-safety-0907-sec-safety-0002-1password`: Analyze and create new helper scripts inside its `scripts/` directory (if applicable)
[x] `sec-safety-0908-sec-safety-0014-active-directory-attacks`: Check if any existing script inside the repository's `scripts/` subfolders would fit as a helper and copy it over (if applicable)
[x] `sec-safety-0908-sec-safety-0014-active-directory-attacks`: Analyze and create new helper scripts inside its `scripts/` directory (if applicable)
[x] `sec-safety-0909-sec-safety-0020-agent-agentic-payments`: Check if any existing script inside the repository's `scripts/` subfolders would fit as a helper and copy it over (if applicable)
[x] `sec-safety-0909-sec-safety-0020-agent-agentic-payments`: Analyze and create new helper scripts inside its `scripts/` directory (if applicable)
[x] `sec-safety-0910-sec-safety-0022-agent-code-review-swarm`: Check if any existing script inside the repository's `scripts/` subfolders would fit as a helper and copy it over (if applicable)
[x] `sec-safety-0910-sec-safety-0022-agent-code-review-swarm`: Analyze and create new helper scripts inside its `scripts/` directory (if applicable)
[x] `sec-safety-0911-sec-safety-0024-agent-github-pr-manager`: Check if any existing script inside the repository's `scripts/` subfolders would fit as a helper and copy it over (if applicable)
[x] `sec-safety-0911-sec-safety-0024-agent-github-pr-manager`: Analyze and create new helper scripts inside its `scripts/` directory (if applicable)
[x] `sec-safety-0912-sec-safety-0026-agent-md-refactor`: Check if any existing script inside the repository's `scripts/` subfolders would fit as a helper and copy it over (if applicable)
[x] `sec-safety-0912-sec-safety-0026-agent-md-refactor`: Analyze and create new helper scripts inside its `scripts/` directory (if applicable)
[x] `sec-safety-0913-sec-safety-0027-agent-migration-plan`: Check if any existing script inside the repository's `scripts/` subfolders would fit as a helper and copy it over (if applicable)
[x] `sec-safety-0913-sec-safety-0027-agent-migration-plan`: Analyze and create new helper scripts inside its `scripts/` directory (if applicable)
[x] `sec-safety-0914-sec-safety-0030-agent-payments`: Check if any existing script inside the repository's `scripts/` subfolders would fit as a helper and copy it over (if applicable)
[x] `sec-safety-0914-sec-safety-0030-agent-payments`: Analyze and create new helper scripts inside its `scripts/` directory (if applicable)
[x] `sec-safety-0915-sec-safety-0033-agent-pr-manager`: Check if any existing script inside the repository's `scripts/` subfolders would fit as a helper and copy it over (if applicable)
[x] `sec-safety-0915-sec-safety-0033-agent-pr-manager`: Analyze and create new helper scripts inside its `scripts/` directory (if applicable)
[x] `sec-safety-0916-sec-safety-0035-agent-trading-predictor`: Check if any existing script inside the repository's `scripts/` subfolders would fit as a helper and copy it over (if applicable)
[x] `sec-safety-0916-sec-safety-0035-agent-trading-predictor`: Analyze and create new helper scripts inside its `scripts/` directory (if applicable)
[x] `sec-safety-0917-sec-safety-0037-agent-workflow`: Check if any existing script inside the repository's `scripts/` subfolders would fit as a helper and copy it over (if applicable)
[x] `sec-safety-0917-sec-safety-0037-agent-workflow`: Analyze and create new helper scripts inside its `scripts/` directory (if applicable)
[x] `sec-safety-0918-sec-safety-0039-agentdb-learning`: Check if any existing script inside the repository's `scripts/` subfolders would fit as a helper and copy it over (if applicable)
[x] `sec-safety-0918-sec-safety-0039-agentdb-learning`: Analyze and create new helper scripts inside its `scripts/` directory (if applicable)
[x] `sec-safety-0920-sec-safety-0054-alphafold-database`: Check if any existing script inside the repository's `scripts/` subfolders would fit as a helper and copy it over (if applicable)
[x] `sec-safety-0920-sec-safety-0054-alphafold-database`: Analyze and create new helper scripts inside its `scripts/` directory (if applicable)
[x] `sec-safety-0921-sec-safety-0060-analyze-with-file`: Check if any existing script inside the repository's `scripts/` subfolders would fit as a helper and copy it over (if applicable)
[x] `sec-safety-0921-sec-safety-0060-analyze-with-file`: Analyze and create new helper scripts inside its `scripts/` directory (if applicable)
[x] `sec-safety-0922-sec-safety-0066-api-fuzzing-bug-bounty`: Check if any existing script inside the repository's `scripts/` subfolders would fit as a helper and copy it over (if applicable)
[x] `sec-safety-0922-sec-safety-0066-api-fuzzing-bug-bounty`: Analyze and create new helper scripts inside its `scripts/` directory (if applicable)
[x] `sec-safety-0923-sec-safety-0069-apple-notes`: Check if any existing script inside the repository's `scripts/` subfolders would fit as a helper and copy it over (if applicable)
[x] `sec-safety-0923-sec-safety-0069-apple-notes`: Analyze and create new helper scripts inside its `scripts/` directory (if applicable)
[x] `sec-safety-0924-sec-safety-0070-apple-reminders`: Check if any existing script inside the repository's `scripts/` subfolders would fit as a helper and copy it over (if applicable)
[x] `sec-safety-0924-sec-safety-0070-apple-reminders`: Analyze and create new helper scripts inside its `scripts/` directory (if applicable)
