# Task 0300: Skill Scripts Batch 015

**Status**: [x]
**Priority**: LOW
**Type**: ops

## Objective
Analyze each skill in this batch and create helper scripts inside their `scripts/` subdirectory where it makes sense, based on the `SKILL.md` instructions.

## Sub-tasks
+ [x] `ai-llm-0311-ai-llm-0890-ralph`: Check if any existing script inside the repository's `scripts/` subfolders would fit as a helper and copy it over (if applicable)
+ [x] `ai-llm-0311-ai-llm-0890-ralph`: Analyze and create new helper scripts inside its `scripts/` directory (if applicable)
+ [x] `ai-llm-0312-ai-llm-0891-rank-tracker`: Check if any existing script inside the repository's `scripts/` subfolders would fit as a helper and copy it over (if applicable)
+ [x] `ai-llm-0312-ai-llm-0891-rank-tracker`: Analyze and create new helper scripts inside its `scripts/` directory (if applicable)
+ [x] `ai-llm-0313-ai-llm-0893-react-best-practices`: Check if any existing script inside the repository's `scripts/` subfolders would fit as a helper and copy it over (if applicable)
+ [x] `ai-llm-0313-ai-llm-0893-react-best-practices`: Analyze and create new helper scripts inside its `scripts/` directory (if applicable)
+ [x] `ai-llm-0314-ai-llm-0894-react-email`: Check if any existing script inside the repository's `scripts/` subfolders would fit as a helper and copy it over (if applicable)
+ [x] `ai-llm-0314-ai-llm-0894-react-email`: Analyze and create new helper scripts inside its `scripts/` directory (if applicable)
+ [x] `ai-llm-0315-ai-llm-0899-read-docs`: Check if any existing script inside the repository's `scripts/` subfolders would fit as a helper and copy it over (if applicable)
+ [x] `ai-llm-0315-ai-llm-0899-read-docs`: Analyze and create new helper scripts inside its `scripts/` directory (if applicable)
+ [x] `ai-llm-0316-ai-llm-0901-reasoningbank-agentdb`: Check if any existing script inside the repository's `scripts/` subfolders would fit as a helper and copy it over (if applicable)
+ [x] `ai-llm-0316-ai-llm-0901-reasoningbank-agentdb`: Analyze and create new helper scripts inside its `scripts/` directory (if applicable)
+ [x] `ai-llm-0317-ai-llm-0902-reasoningbank-intelligence`: Check if any existing script inside the repository's `scripts/` subfolders would fit as a helper and copy it over (if applicable)
+ [x] `ai-llm-0317-ai-llm-0902-reasoningbank-intelligence`: Analyze and create new helper scripts inside its `scripts/` directory (if applicable)
+ [x] `ai-llm-0318-ai-llm-0903-recall-reasoning`: Check if any existing script inside the repository's `scripts/` subfolders would fit as a helper and copy it over (if applicable)
+ [x] `ai-llm-0318-ai-llm-0903-recall-reasoning`: Analyze and create new helper scripts inside its `scripts/` directory (if applicable)
+ [x] `ai-llm-0319-ai-llm-0907-reddapi`: Check if any existing script inside the repository's `scripts/` subfolders would fit as a helper and copy it over (if applicable)
+ [x] `ai-llm-0319-ai-llm-0907-reddapi`: Analyze and create new helper scripts inside its `scripts/` directory (if applicable)
+ [x] `ai-llm-0320-ai-llm-0908-reference`: Check if any existing script inside the repository's `scripts/` subfolders would fit as a helper and copy it over (if applicable)
+ [x] `ai-llm-0320-ai-llm-0908-reference`: Analyze and create new helper scripts inside its `scripts/` directory (if applicable)
+ [x] `ai-llm-0321-ai-llm-0909-referral-program`: Check if any existing script inside the repository's `scripts/` subfolders would fit as a helper and copy it over (if applicable)
+ [x] `ai-llm-0321-ai-llm-0909-referral-program`: Analyze and create new helper scripts inside its `scripts/` directory (if applicable)
+ [x] `ai-llm-0322-ai-llm-0912-release-note-generation`: Check if any existing script inside the repository's `scripts/` subfolders would fit as a helper and copy it over (if applicable)
+ [x] `ai-llm-0322-ai-llm-0912-release-note-generation`: Analyze and create new helper scripts inside its `scripts/` directory (if applicable)
+ [x] `ai-llm-0323-ai-llm-0917-research`: Check if any existing script inside the repository's `scripts/` subfolders would fit as a helper and copy it over (if applicable)
+ [x] `ai-llm-0323-ai-llm-0917-research`: Analyze and create new helper scripts inside its `scripts/` directory (if applicable)
+ [x] `ai-llm-0324-ai-llm-0918-research-company`: Check if any existing script inside the repository's `scripts/` subfolders would fit as a helper and copy it over (if applicable)
+ [x] `ai-llm-0324-ai-llm-0918-research-company`: Analyze and create new helper scripts inside its `scripts/` directory (if applicable)
+ [x] `ai-llm-0325-ai-llm-0923-researchmonitor`: Check if any existing script inside the repository's `scripts/` subfolders would fit as a helper and copy it over (if applicable)
+ [x] `ai-llm-0325-ai-llm-0923-researchmonitor`: Analyze and create new helper scripts inside its `scripts/` directory (if applicable)
+ [x] `ai-llm-0326-ai-llm-0924-residues`: Check if any existing script inside the repository's `scripts/` subfolders would fit as a helper and copy it over (if applicable)
+ [x] `ai-llm-0326-ai-llm-0924-residues`: Analyze and create new helper scripts inside its `scripts/` directory (if applicable)
+ [x] `ai-llm-0327-ai-llm-0925-response-drafting`: Check if any existing script inside the repository's `scripts/` subfolders would fit as a helper and copy it over (if applicable)
+ [x] `ai-llm-0327-ai-llm-0925-response-drafting`: Analyze and create new helper scripts inside its `scripts/` directory (if applicable)
+ [x] `ai-llm-0328-ai-llm-0926-resume-builder`: Check if any existing script inside the repository's `scripts/` subfolders would fit as a helper and copy it over (if applicable)
+ [x] `ai-llm-0328-ai-llm-0926-resume-builder`: Analyze and create new helper scripts inside its `scripts/` directory (if applicable)
+ [x] `ai-llm-0329-ai-llm-0932-review-summarizer`: Check if any existing script inside the repository's `scripts/` subfolders would fit as a helper and copy it over (if applicable)
+ [x] `ai-llm-0329-ai-llm-0932-review-summarizer`: Analyze and create new helper scripts inside its `scripts/` directory (if applicable)
+ [x] `ai-llm-0330-ai-llm-0937-root-finding`: Check if any existing script inside the repository's `scripts/` subfolders would fit as a helper and copy it over (if applicable)
+ [x] `ai-llm-0330-ai-llm-0937-root-finding`: Analyze and create new helper scripts inside its `scripts/` directory (if applicable)
