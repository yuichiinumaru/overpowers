# Task 0300: Skill Scripts Batch 055

**Status**: [ ]
**Priority**: LOW
**Type**: ops

## Objective
Analyze each skill in this batch and create helper scripts inside their `scripts/` subdirectory where it makes sense, based on the `SKILL.md` instructions.

## Sub-tasks
- [ ] `sec-safety-1135-sec-safety-1170-v3-swarm-coordination`: Check if any existing script inside the repository's `scripts/` subfolders would fit as a helper and copy it over (if applicable)
- [ ] `sec-safety-1135-sec-safety-1170-v3-swarm-coordination`: Analyze and create new helper scripts inside its `scripts/` directory (if applicable)
- [ ] `sec-safety-1137-sec-safety-1179-vibe-coding-best-practices`: Check if any existing script inside the repository's `scripts/` subfolders would fit as a helper and copy it over (if applicable)
- [ ] `sec-safety-1137-sec-safety-1179-vibe-coding-best-practices`: Analyze and create new helper scripts inside its `scripts/` directory (if applicable)
- [ ] `sec-safety-1138-sec-safety-1217-worker-integration`: Check if any existing script inside the repository's `scripts/` subfolders would fit as a helper and copy it over (if applicable)
- [ ] `sec-safety-1138-sec-safety-1217-worker-integration`: Analyze and create new helper scripts inside its `scripts/` directory (if applicable)
- [ ] `sec-safety-1139-sec-safety-1235-xss-html-injection`: Check if any existing script inside the repository's `scripts/` subfolders would fit as a helper and copy it over (if applicable)
- [ ] `sec-safety-1139-sec-safety-1235-xss-html-injection`: Analyze and create new helper scripts inside its `scripts/` directory (if applicable)
- [ ] `sec-safety-1140-sec-safety-1236-yara-rule-authoring`: Check if any existing script inside the repository's `scripts/` subfolders would fit as a helper and copy it over (if applicable)
- [ ] `sec-safety-1140-sec-safety-1236-yara-rule-authoring`: Analyze and create new helper scripts inside its `scripts/` directory (if applicable)
- [ ] `sec-safety-1141-sec-safety-1241-youtube-transcribe-skill`: Check if any existing script inside the repository's `scripts/` subfolders would fit as a helper and copy it over (if applicable)
- [ ] `sec-safety-1141-sec-safety-1241-youtube-transcribe-skill`: Analyze and create new helper scripts inside its `scripts/` directory (if applicable)
- [ ] `sec-safety-1142-sec-safety-1242-z-audit`: Check if any existing script inside the repository's `scripts/` subfolders would fit as a helper and copy it over (if applicable)
- [ ] `sec-safety-1142-sec-safety-1242-z-audit`: Analyze and create new helper scripts inside its `scripts/` directory (if applicable)
- [ ] `sec-safety-1143-security-best-practices`: Check if any existing script inside the repository's `scripts/` subfolders would fit as a helper and copy it over (if applicable)
- [ ] `sec-safety-1143-security-best-practices`: Analyze and create new helper scripts inside its `scripts/` directory (if applicable)
- [ ] `tool-general-1144-tool-general-0366-editor`: Check if any existing script inside the repository's `scripts/` subfolders would fit as a helper and copy it over (if applicable)
- [ ] `tool-general-1144-tool-general-0366-editor`: Analyze and create new helper scripts inside its `scripts/` directory (if applicable)
- [ ] `tool-general-1145-tool-general-0508-greeter`: Check if any existing script inside the repository's `scripts/` subfolders would fit as a helper and copy it over (if applicable)
- [ ] `tool-general-1145-tool-general-0508-greeter`: Analyze and create new helper scripts inside its `scripts/` directory (if applicable)
- [ ] `tool-general-1146-tool-general-0691-multiline-description`: Check if any existing script inside the repository's `scripts/` subfolders would fit as a helper and copy it over (if applicable)
- [ ] `tool-general-1146-tool-general-0691-multiline-description`: Analyze and create new helper scripts inside its `scripts/` directory (if applicable)
- [ ] `tool-general-1147-tool-general-0692-mvp-validation-process`: Check if any existing script inside the repository's `scripts/` subfolders would fit as a helper and copy it over (if applicable)
- [ ] `tool-general-1147-tool-general-0692-mvp-validation-process`: Analyze and create new helper scripts inside its `scripts/` directory (if applicable)
- [ ] `tool-general-1148-tool-general-0821-prevc-bug-investigation`: Check if any existing script inside the repository's `scripts/` subfolders would fit as a helper and copy it over (if applicable)
- [ ] `tool-general-1148-tool-general-0821-prevc-bug-investigation`: Analyze and create new helper scripts inside its `scripts/` directory (if applicable)
- [ ] `tool-general-1149-tool-general-0823-prevc-commit-message`: Check if any existing script inside the repository's `scripts/` subfolders would fit as a helper and copy it over (if applicable)
- [ ] `tool-general-1149-tool-general-0823-prevc-commit-message`: Analyze and create new helper scripts inside its `scripts/` directory (if applicable)
- [ ] `tool-general-1150-tool-general-0825-prevc-feature-breakdown`: Check if any existing script inside the repository's `scripts/` subfolders would fit as a helper and copy it over (if applicable)
- [ ] `tool-general-1150-tool-general-0825-prevc-feature-breakdown`: Analyze and create new helper scripts inside its `scripts/` directory (if applicable)
- [ ] `tool-general-1151-tool-general-0928-reverse-engineering`: Check if any existing script inside the repository's `scripts/` subfolders would fit as a helper and copy it over (if applicable)
- [ ] `tool-general-1151-tool-general-0928-reverse-engineering`: Analyze and create new helper scripts inside its `scripts/` directory (if applicable)
- [ ] `tool-general-1152-tool-general-1133-track-management`: Check if any existing script inside the repository's `scripts/` subfolders would fit as a helper and copy it over (if applicable)
- [ ] `tool-general-1152-tool-general-1133-track-management`: Analyze and create new helper scripts inside its `scripts/` directory (if applicable)
- [ ] `tool-general-1153-tool-general-1238-youtube-skill-creator`: Check if any existing script inside the repository's `scripts/` subfolders would fit as a helper and copy it over (if applicable)
- [ ] `tool-general-1153-tool-general-1238-youtube-skill-creator`: Analyze and create new helper scripts inside its `scripts/` directory (if applicable)
- [ ] `ux-design-1154-ux-design-0004-8-bit-pixel-art-patterns`: Check if any existing script inside the repository's `scripts/` subfolders would fit as a helper and copy it over (if applicable)
- [ ] `ux-design-1154-ux-design-0004-8-bit-pixel-art-patterns`: Analyze and create new helper scripts inside its `scripts/` directory (if applicable)
- [ ] `ux-design-1155-ux-design-0011-accessibility-compliance-accessibility-audit`: Check if any existing script inside the repository's `scripts/` subfolders would fit as a helper and copy it over (if applicable)
- [ ] `ux-design-1155-ux-design-0011-accessibility-compliance-accessibility-audit`: Analyze and create new helper scripts inside its `scripts/` directory (if applicable)
