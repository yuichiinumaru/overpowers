# Task 0300: Skill Scripts Batch 033

**Status**: [ ]
**Priority**: LOW
**Type**: ops

## Objective
Analyze each skill in this batch and create helper scripts inside their `scripts/` subdirectory where it makes sense, based on the `SKILL.md` instructions.

## Sub-tasks
- [x] `ops-infra-0672-ops-infra-0748-openspec-explore`: Check if any existing script inside the repository's `scripts/` subfolders would fit as a helper and copy it over (if applicable)
- [x] `ops-infra-0672-ops-infra-0748-openspec-explore`: Analyze and create new helper scripts inside its `scripts/` directory (if applicable)
- [x] `ops-infra-0673-ops-infra-0758-outlook-calendar-automation`: Check if any existing script inside the repository's `scripts/` subfolders would fit as a helper and copy it over (if applicable)
- [x] `ops-infra-0673-ops-infra-0758-outlook-calendar-automation`: Analyze and create new helper scripts inside its `scripts/` directory (if applicable)
- [x] `ops-infra-0674-ops-infra-0761-pagerduty-automation`: Check if any existing script inside the repository's `scripts/` subfolders would fit as a helper and copy it over (if applicable)
- [x] `ops-infra-0674-ops-infra-0761-pagerduty-automation`: Analyze and create new helper scripts inside its `scripts/` directory (if applicable)
- [x] `ops-infra-0675-ops-infra-0781-pdf-processing-pro`: Check if any existing script inside the repository's `scripts/` subfolders would fit as a helper and copy it over (if applicable)
- [x] `ops-infra-0675-ops-infra-0781-pdf-processing-pro`: Analyze and create new helper scripts inside its `scripts/` directory (if applicable)
- [x] `ops-infra-0676-ops-infra-0796-pipedrive-automation`: Check if any existing script inside the repository's `scripts/` subfolders would fit as a helper and copy it over (if applicable)
- [x] `ops-infra-0676-ops-infra-0796-pipedrive-automation`: Analyze and create new helper scripts inside its `scripts/` directory (if applicable)
- [x] `ops-infra-0677-ops-infra-0797-plan-converter`: Check if any existing script inside the repository's `scripts/` subfolders would fit as a helper and copy it over (if applicable)
- [x] `ops-infra-0677-ops-infra-0797-plan-converter`: Analyze and create new helper scripts inside its `scripts/` directory (if applicable)
- [x] `ops-infra-0678-ops-infra-0800-plantuml-ascii`: Check if any existing script inside the repository's `scripts/` subfolders would fit as a helper and copy it over (if applicable)
- [x] `ops-infra-0678-ops-infra-0800-plantuml-ascii`: Analyze and create new helper scripts inside its `scripts/` directory (if applicable)
- [x] `ops-infra-0679-ops-infra-0813-pr-build-status`: Check if any existing script inside the repository's `scripts/` subfolders would fit as a helper and copy it over (if applicable)
- [x] `ops-infra-0679-ops-infra-0813-pr-build-status`: Analyze and create new helper scripts inside its `scripts/` directory (if applicable)
- [x] `ops-infra-0680-ops-infra-0814-pr-creator`: Check if any existing script inside the repository's `scripts/` subfolders would fit as a helper and copy it over (if applicable)
- [x] `ops-infra-0680-ops-infra-0814-pr-creator`: Analyze and create new helper scripts inside its `scripts/` directory (if applicable)
- [x] `ops-infra-0681-ops-infra-0819-prepare-pr`: Check if any existing script inside the repository's `scripts/` subfolders would fit as a helper and copy it over (if applicable)
- [x] `ops-infra-0681-ops-infra-0819-prepare-pr`: Analyze and create new helper scripts inside its `scripts/` directory (if applicable)
- [x] `ops-infra-0683-ops-infra-0840-progress`: Check if any existing script inside the repository's `scripts/` subfolders would fit as a helper and copy it over (if applicable)
- [x] `ops-infra-0683-ops-infra-0840-progress`: Analyze and create new helper scripts inside its `scripts/` directory (if applicable)
- [x] `ops-infra-0684-ops-infra-0843-project-planner`: Check if any existing script inside the repository's `scripts/` subfolders would fit as a helper and copy it over (if applicable)
- [x] `ops-infra-0684-ops-infra-0843-project-planner`: Analyze and create new helper scripts inside its `scripts/` directory (if applicable)
- [x] `ops-infra-0685-ops-infra-0844-prometheus-configuration`: Check if any existing script inside the repository's `scripts/` subfolders would fit as a helper and copy it over (if applicable)
- [x] `ops-infra-0685-ops-infra-0844-prometheus-configuration`: Analyze and create new helper scripts inside its `scripts/` directory (if applicable)
- [x] `ops-infra-0686-ops-infra-0865-python-development`: Check if any existing script inside the repository's `scripts/` subfolders would fit as a helper and copy it over (if applicable)
- [x] `ops-infra-0686-ops-infra-0865-python-development`: Analyze and create new helper scripts inside its `scripts/` directory (if applicable)
- [x] `ops-infra-0687-ops-infra-0874-quality-manager-qmr`: Check if any existing script inside the repository's `scripts/` subfolders would fit as a helper and copy it over (if applicable)
- [x] `ops-infra-0687-ops-infra-0874-quality-manager-qmr`: Analyze and create new helper scripts inside its `scripts/` directory (if applicable)
- [x] `ops-infra-0688-ops-infra-0875-quality-manager-qms-iso13485`: Check if any existing script inside the repository's `scripts/` subfolders would fit as a helper and copy it over (if applicable)
- [x] `ops-infra-0688-ops-infra-0875-quality-manager-qms-iso13485`: Analyze and create new helper scripts inside its `scripts/` directory (if applicable)
- [x] `ops-infra-0689-ops-infra-0885-railway-deployment`: Check if any existing script inside the repository's `scripts/` subfolders would fit as a helper and copy it over (if applicable)
- [x] `ops-infra-0689-ops-infra-0885-railway-deployment`: Analyze and create new helper scripts inside its `scripts/` directory (if applicable)
- [x] `ops-infra-0690-ops-infra-0886-railway-environment`: Check if any existing script inside the repository's `scripts/` subfolders would fit as a helper and copy it over (if applicable)
- [x] `ops-infra-0690-ops-infra-0886-railway-environment`: Analyze and create new helper scripts inside its `scripts/` directory (if applicable)
- [x] `ops-infra-0691-ops-infra-0887-railway-metrics`: Check if any existing script inside the repository's `scripts/` subfolders would fit as a helper and copy it over (if applicable)
- [x] `ops-infra-0691-ops-infra-0887-railway-metrics`: Analyze and create new helper scripts inside its `scripts/` directory (if applicable)
- [x] `ops-infra-0692-ops-infra-0889-railway-status`: Check if any existing script inside the repository's `scripts/` subfolders would fit as a helper and copy it over (if applicable)
- [x] `ops-infra-0692-ops-infra-0889-railway-status`: Analyze and create new helper scripts inside its `scripts/` directory (if applicable)
