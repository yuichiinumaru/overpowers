# Task 0300: Skill Scripts Batch 058

**Status**: [ ]
**Priority**: LOW
**Type**: ops

## Objective
Analyze each skill in this batch and create helper scripts inside their `scripts/` subdirectory where it makes sense, based on the `SKILL.md` instructions.

## Sub-tasks
- [ ] `web-frontend-1198-web-frontend-0135-bear-notes`: Check if any existing script inside the repository's `scripts/` subfolders would fit as a helper and copy it over (if applicable)
- [ ] `web-frontend-1198-web-frontend-0135-bear-notes`: Analyze and create new helper scripts inside its `scripts/` directory (if applicable)
- [ ] `web-frontend-1199-web-frontend-0150-bluebubbles`: Check if any existing script inside the repository's `scripts/` subfolders would fit as a helper and copy it over (if applicable)
- [ ] `web-frontend-1199-web-frontend-0150-bluebubbles`: Analyze and create new helper scripts inside its `scripts/` directory (if applicable)
- [ ] `web-frontend-1200-web-frontend-0229-code-refactor`: Check if any existing script inside the repository's `scripts/` subfolders would fit as a helper and copy it over (if applicable)
- [ ] `web-frontend-1200-web-frontend-0229-code-refactor`: Analyze and create new helper scripts inside its `scripts/` directory (if applicable)
- [ ] `web-frontend-1204-web-frontend-0316-datadog-cli`: Check if any existing script inside the repository's `scripts/` subfolders would fit as a helper and copy it over (if applicable)
- [ ] `web-frontend-1204-web-frontend-0316-datadog-cli`: Analyze and create new helper scripts inside its `scripts/` directory (if applicable)
- [ ] `web-frontend-1205-web-frontend-0398-extract-errors`: Check if any existing script inside the repository's `scripts/` subfolders would fit as a helper and copy it over (if applicable)
- [ ] `web-frontend-1205-web-frontend-0398-extract-errors`: Analyze and create new helper scripts inside its `scripts/` directory (if applicable)
- [ ] `web-frontend-1206-web-frontend-0408-feature-flags`: Check if any existing script inside the repository's `scripts/` subfolders would fit as a helper and copy it over (if applicable)
- [ ] `web-frontend-1206-web-frontend-0408-feature-flags`: Analyze and create new helper scripts inside its `scripts/` directory (if applicable)
- [ ] `web-frontend-1207-web-frontend-0423-firecrawl-scrape`: Check if any existing script inside the repository's `scripts/` subfolders would fit as a helper and copy it over (if applicable)
- [ ] `web-frontend-1207-web-frontend-0423-firecrawl-scrape`: Analyze and create new helper scripts inside its `scripts/` directory (if applicable)
- [ ] `web-frontend-1208-web-frontend-0427-flags`: Check if any existing script inside the repository's `scripts/` subfolders would fit as a helper and copy it over (if applicable)
- [ ] `web-frontend-1208-web-frontend-0427-flags`: Analyze and create new helper scripts inside its `scripts/` directory (if applicable)
- [ ] `web-frontend-1209-web-frontend-0429-flow`: Check if any existing script inside the repository's `scripts/` subfolders would fit as a helper and copy it over (if applicable)
- [ ] `web-frontend-1209-web-frontend-0429-flow`: Analyze and create new helper scripts inside its `scripts/` directory (if applicable)
- [ ] `web-frontend-1210-web-frontend-0543-i18n`: Check if any existing script inside the repository's `scripts/` subfolders would fit as a helper and copy it over (if applicable)
- [ ] `web-frontend-1210-web-frontend-0543-i18n`: Analyze and create new helper scripts inside its `scripts/` directory (if applicable)
- [ ] `web-frontend-1211-web-frontend-0579-javascript-typescript`: Check if any existing script inside the repository's `scripts/` subfolders would fit as a helper and copy it over (if applicable)
- [ ] `web-frontend-1211-web-frontend-0579-javascript-typescript`: Analyze and create new helper scripts inside its `scripts/` directory (if applicable)
- [ ] `web-frontend-1212-web-frontend-0609-linear`: Check if any existing script inside the repository's `scripts/` subfolders would fit as a helper and copy it over (if applicable)
- [ ] `web-frontend-1212-web-frontend-0609-linear`: Analyze and create new helper scripts inside its `scripts/` directory (if applicable)
- [ ] `web-frontend-1213-web-frontend-0624-local-places`: Check if any existing script inside the repository's `scripts/` subfolders would fit as a helper and copy it over (if applicable)
- [ ] `web-frontend-1213-web-frontend-0624-local-places`: Analyze and create new helper scripts inside its `scripts/` directory (if applicable)
- [ ] `web-frontend-1214-web-frontend-0714-nlb`: Check if any existing script inside the repository's `scripts/` subfolders would fit as a helper and copy it over (if applicable)
- [ ] `web-frontend-1214-web-frontend-0714-nlb`: Analyze and create new helper scripts inside its `scripts/` directory (if applicable)
- [ ] `web-frontend-1215-web-frontend-0755-orpc-contract-first`: Check if any existing script inside the repository's `scripts/` subfolders would fit as a helper and copy it over (if applicable)
- [ ] `web-frontend-1215-web-frontend-0755-orpc-contract-first`: Analyze and create new helper scripts inside its `scripts/` directory (if applicable)
- [ ] `web-frontend-1216-web-frontend-0824-prevc-documentation`: Check if any existing script inside the repository's `scripts/` subfolders would fit as a helper and copy it over (if applicable)
- [ ] `web-frontend-1216-web-frontend-0824-prevc-documentation`: Analyze and create new helper scripts inside its `scripts/` directory (if applicable)
- [ ] `web-frontend-1217-web-frontend-0868-pyzotero-cli`: Check if any existing script inside the repository's `scripts/` subfolders would fit as a helper and copy it over (if applicable)
- [ ] `web-frontend-1217-web-frontend-0868-pyzotero-cli`: Analyze and create new helper scripts inside its `scripts/` directory (if applicable)
- [ ] `web-frontend-1218-web-frontend-0888-railway-projects`: Check if any existing script inside the repository's `scripts/` subfolders would fit as a helper and copy it over (if applicable)
- [ ] `web-frontend-1218-web-frontend-0888-railway-projects`: Analyze and create new helper scripts inside its `scripts/` directory (if applicable)
- [ ] `web-frontend-1219-web-frontend-0929-review-book`: Check if any existing script inside the repository's `scripts/` subfolders would fit as a helper and copy it over (if applicable)
- [ ] `web-frontend-1219-web-frontend-0929-review-book`: Analyze and create new helper scripts inside its `scripts/` directory (if applicable)
- [ ] `web-frontend-1220-web-frontend-0965-searxng-search`: Check if any existing script inside the repository's `scripts/` subfolders would fit as a helper and copy it over (if applicable)
- [ ] `web-frontend-1220-web-frontend-0965-searxng-search`: Analyze and create new helper scripts inside its `scripts/` directory (if applicable)
