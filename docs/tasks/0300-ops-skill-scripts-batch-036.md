# Task 0300: Skill Scripts Batch 036

**Status**: [ ]
**Priority**: LOW
**Type**: ops

## Objective
Analyze each skill in this batch and create helper scripts inside their `scripts/` subdirectory where it makes sense, based on the `SKILL.md` instructions.

## Sub-tasks
- [ ] `ops-infra-0735-netlify-deploy`: Check if any existing script inside the repository's `scripts/` subfolders would fit as a helper and copy it over (if applicable)
- [ ] `ops-infra-0735-netlify-deploy`: Analyze and create new helper scripts inside its `scripts/` directory (if applicable)
- [ ] `ops-infra-0736-render-deploy`: Check if any existing script inside the repository's `scripts/` subfolders would fit as a helper and copy it over (if applicable)
- [ ] `ops-infra-0736-render-deploy`: Analyze and create new helper scripts inside its `scripts/` directory (if applicable)
- [ ] `ops-infra-0738-yeet`: Check if any existing script inside the repository's `scripts/` subfolders would fit as a helper and copy it over (if applicable)
- [ ] `ops-infra-0738-yeet`: Analyze and create new helper scripts inside its `scripts/` directory (if applicable)
- [ ] `sci-bio-0734-sci-bio-0006-academic-deep-research`: Check if any existing script inside the repository's `scripts/` subfolders would fit as a helper and copy it over (if applicable)
- [ ] `sci-bio-0734-sci-bio-0006-academic-deep-research`: Analyze and create new helper scripts inside its `scripts/` directory (if applicable)
- [ ] `sci-bio-0735-sci-bio-0016-adaptyv`: Check if any existing script inside the repository's `scripts/` subfolders would fit as a helper and copy it over (if applicable)
- [ ] `sci-bio-0735-sci-bio-0016-adaptyv`: Analyze and create new helper scripts inside its `scripts/` directory (if applicable)
- [ ] `sci-bio-0736-sci-bio-0072-arboreto`: Check if any existing script inside the repository's `scripts/` subfolders would fit as a helper and copy it over (if applicable)
- [ ] `sci-bio-0736-sci-bio-0072-arboreto`: Analyze and create new helper scripts inside its `scripts/` directory (if applicable)
- [ ] `sci-bio-0737-sci-bio-0140-biopython`: Check if any existing script inside the repository's `scripts/` subfolders would fit as a helper and copy it over (if applicable)
- [ ] `sci-bio-0737-sci-bio-0140-biopython`: Analyze and create new helper scripts inside its `scripts/` directory (if applicable)
- [ ] `sci-bio-0738-sci-bio-0141-biorxiv-database`: Check if any existing script inside the repository's `scripts/` subfolders would fit as a helper and copy it over (if applicable)
- [ ] `sci-bio-0738-sci-bio-0141-biorxiv-database`: Analyze and create new helper scripts inside its `scripts/` directory (if applicable)
- [ ] `sci-bio-0739-sci-bio-0142-bioservices`: Check if any existing script inside the repository's `scripts/` subfolders would fit as a helper and copy it over (if applicable)
- [ ] `sci-bio-0739-sci-bio-0142-bioservices`: Analyze and create new helper scripts inside its `scripts/` directory (if applicable)
- [ ] `sci-bio-0740-sci-bio-0167-bulk-rna-seq-batch-correction-with-combat`: Check if any existing script inside the repository's `scripts/` subfolders would fit as a helper and copy it over (if applicable)
- [ ] `sci-bio-0740-sci-bio-0167-bulk-rna-seq-batch-correction-with-combat`: Analyze and create new helper scripts inside its `scripts/` directory (if applicable)
- [ ] `sci-bio-0741-sci-bio-0168-bulk-rna-seq-deconvolution-with-bulk2single`: Check if any existing script inside the repository's `scripts/` subfolders would fit as a helper and copy it over (if applicable)
- [ ] `sci-bio-0741-sci-bio-0168-bulk-rna-seq-deconvolution-with-bulk2single`: Analyze and create new helper scripts inside its `scripts/` directory (if applicable)
- [ ] `sci-bio-0742-sci-bio-0169-bulk-rna-seq-deseq2-analysis-with-omicverse`: Check if any existing script inside the repository's `scripts/` subfolders would fit as a helper and copy it over (if applicable)
- [ ] `sci-bio-0742-sci-bio-0169-bulk-rna-seq-deseq2-analysis-with-omicverse`: Analyze and create new helper scripts inside its `scripts/` directory (if applicable)
- [ ] `sci-bio-0743-sci-bio-0170-bulk-rna-seq-differential-expression-with-omicverse`: Check if any existing script inside the repository's `scripts/` subfolders would fit as a helper and copy it over (if applicable)
- [ ] `sci-bio-0743-sci-bio-0170-bulk-rna-seq-differential-expression-with-omicverse`: Analyze and create new helper scripts inside its `scripts/` directory (if applicable)
- [ ] `sci-bio-0744-sci-bio-0171-bulk-wgcna-analysis-with-omicverse`: Check if any existing script inside the repository's `scripts/` subfolders would fit as a helper and copy it over (if applicable)
- [ ] `sci-bio-0744-sci-bio-0171-bulk-wgcna-analysis-with-omicverse`: Analyze and create new helper scripts inside its `scripts/` directory (if applicable)
- [ ] `sci-bio-0745-sci-bio-0172-bulktrajblend-trajectory-interpolation`: Check if any existing script inside the repository's `scripts/` subfolders would fit as a helper and copy it over (if applicable)
- [ ] `sci-bio-0745-sci-bio-0172-bulktrajblend-trajectory-interpolation`: Analyze and create new helper scripts inside its `scripts/` directory (if applicable)
- [ ] `sci-bio-0746-sci-bio-0194-cellxgene-census`: Check if any existing script inside the repository's `scripts/` subfolders would fit as a helper and copy it over (if applicable)
- [ ] `sci-bio-0746-sci-bio-0194-cellxgene-census`: Analyze and create new helper scripts inside its `scripts/` directory (if applicable)
- [ ] `sci-bio-0747-sci-bio-0219-clinvar-database`: Check if any existing script inside the repository's `scripts/` subfolders would fit as a helper and copy it over (if applicable)
- [ ] `sci-bio-0747-sci-bio-0219-clinvar-database`: Analyze and create new helper scripts inside its `scripts/` directory (if applicable)
- [ ] `sci-bio-0748-sci-bio-0224-cobrapy`: Check if any existing script inside the repository's `scripts/` subfolders would fit as a helper and copy it over (if applicable)
- [ ] `sci-bio-0748-sci-bio-0224-cobrapy`: Analyze and create new helper scripts inside its `scripts/` directory (if applicable)
- [ ] `sci-bio-0749-sci-bio-0303-data-export-excel`: Check if any existing script inside the repository's `scripts/` subfolders would fit as a helper and copy it over (if applicable)
- [ ] `sci-bio-0749-sci-bio-0303-data-export-excel`: Analyze and create new helper scripts inside its `scripts/` directory (if applicable)
- [ ] `sci-bio-0750-sci-bio-0304-data-export-pdf`: Check if any existing script inside the repository's `scripts/` subfolders would fit as a helper and copy it over (if applicable)
- [ ] `sci-bio-0750-sci-bio-0304-data-export-pdf`: Analyze and create new helper scripts inside its `scripts/` directory (if applicable)
