# Task 0300: Skill Scripts Batch 037

**Status**: [x]
**Priority**: LOW
**Type**: ops

## Objective
Analyze each skill in this batch and create helper scripts inside their `scripts/` subdirectory where it makes sense, based on the `SKILL.md` instructions.

## Sub-tasks
- [x] `sci-bio-0751-sci-bio-0309-data-stats-analysis`: Check if any existing script inside the repository's `scripts/` subfolders would fit as a helper and copy it over (if applicable)
- [x] `sci-bio-0751-sci-bio-0309-data-stats-analysis`: Analyze and create new helper scripts inside its `scripts/` directory (if applicable)
- [x] `sci-bio-0752-sci-bio-0311-data-transform`: Check if any existing script inside the repository's `scripts/` subfolders would fit as a helper and copy it over (if applicable)
- [x] `sci-bio-0752-sci-bio-0311-data-transform`: Analyze and create new helper scripts inside its `scripts/` directory (if applicable)
- [x] `sci-bio-0753-sci-bio-0313-data-viz-plots`: Check if any existing script inside the repository's `scripts/` subfolders would fit as a helper and copy it over (if applicable)
- [x] `sci-bio-0753-sci-bio-0313-data-viz-plots`: Analyze and create new helper scripts inside its `scripts/` directory (if applicable)
- [x] `sci-bio-0754-sci-bio-0325-deeptools`: Check if any existing script inside the repository's `scripts/` subfolders would fit as a helper and copy it over (if applicable)
- [x] `sci-bio-0754-sci-bio-0325-deeptools`: Analyze and create new helper scripts inside its `scripts/` directory (if applicable)
- [x] `sci-bio-0755-sci-bio-0375-ena-database`: Check if any existing script inside the repository's `scripts/` subfolders would fit as a helper and copy it over (if applicable)
- [x] `sci-bio-0755-sci-bio-0375-ena-database`: Analyze and create new helper scripts inside its `scripts/` directory (if applicable)
- [x] `sci-bio-0756-sci-bio-0377-ensembl-database`: Check if any existing script inside the repository's `scripts/` subfolders would fit as a helper and copy it over (if applicable)
- [x] `sci-bio-0756-sci-bio-0377-ensembl-database`: Analyze and create new helper scripts inside its `scripts/` directory (if applicable)
- [x] `sci-bio-0757-sci-bio-0381-etetoolkit`: Check if any existing script inside the repository's `scripts/` subfolders would fit as a helper and copy it over (if applicable)
- [x] `sci-bio-0757-sci-bio-0381-etetoolkit`: Analyze and create new helper scripts inside its `scripts/` directory (if applicable)
- [x] `sci-bio-0758-sci-bio-0466-gene-database`: Check if any existing script inside the repository's `scripts/` subfolders would fit as a helper and copy it over (if applicable)
- [x] `sci-bio-0758-sci-bio-0466-gene-database`: Analyze and create new helper scripts inside its `scripts/` directory (if applicable)
- [x] `sci-bio-0759-sci-bio-0467-generate-image`: Check if any existing script inside the repository's `scripts/` subfolders would fit as a helper and copy it over (if applicable)
- [x] `sci-bio-0759-sci-bio-0467-generate-image`: Analyze and create new helper scripts inside its `scripts/` directory (if applicable)
- [x] `sci-bio-0760-sci-bio-0473-get-available-resources`: Check if any existing script inside the repository's `scripts/` subfolders would fit as a helper and copy it over (if applicable)
- [x] `sci-bio-0760-sci-bio-0473-get-available-resources`: Analyze and create new helper scripts inside its `scripts/` directory (if applicable)
- [x] `sci-bio-0761-sci-bio-0509-gsea-enrichment-analysis`: Check if any existing script inside the repository's `scripts/` subfolders would fit as a helper and copy it over (if applicable)
- [x] `sci-bio-0761-sci-bio-0509-gsea-enrichment-analysis`: Analyze and create new helper scripts inside its `scripts/` directory (if applicable)
- [x] `sci-bio-0762-sci-bio-0510-gtars`: Check if any existing script inside the repository's `scripts/` subfolders would fit as a helper and copy it over (if applicable)
- [x] `sci-bio-0762-sci-bio-0510-gtars`: Analyze and create new helper scripts inside its `scripts/` directory (if applicable)
- [x] `sci-bio-0763-sci-bio-0512-gwas-database`: Check if any existing script inside the repository's `scripts/` subfolders would fit as a helper and copy it over (if applicable)
- [x] `sci-bio-0763-sci-bio-0512-gwas-database`: Analyze and create new helper scripts inside its `scripts/` directory (if applicable)
- [x] `sci-bio-0764-sci-bio-0564-instrument-data-to-allotrope`: Check if any existing script inside the repository's `scripts/` subfolders would fit as a helper and copy it over (if applicable)
- [x] `sci-bio-0764-sci-bio-0564-instrument-data-to-allotrope`: Analyze and create new helper scripts inside its `scripts/` directory (if applicable)
- [x] `sci-bio-0765-sci-bio-0669-metabolomics-workbench-database`: Check if any existing script inside the repository's `scripts/` subfolders would fit as a helper and copy it over (if applicable)
- [x] `sci-bio-0765-sci-bio-0669-metabolomics-workbench-database`: Analyze and create new helper scripts inside its `scripts/` directory (if applicable)
- [x] `sci-bio-0766-sci-bio-0711-nextflow-development`: Check if any existing script inside the repository's `scripts/` subfolders would fit as a helper and copy it over (if applicable)
- [x] `sci-bio-0766-sci-bio-0711-nextflow-development`: Analyze and create new helper scripts inside its `scripts/` directory (if applicable)
- [x] `sci-bio-0767-sci-bio-0735-omicverse-visualization-for-bulk-color-systems-and-single-cell-d`: Check if any existing script inside the repository's `scripts/` subfolders would fit as a helper and copy it over (if applicable)
- [x] `sci-bio-0767-sci-bio-0735-omicverse-visualization-for-bulk-color-systems-and-single-cell-d`: Analyze and create new helper scripts inside its `scripts/` directory (if applicable)
- [x] `sci-bio-0768-sci-bio-0771-pathml`: Check if any existing script inside the repository's `scripts/` subfolders would fit as a helper and copy it over (if applicable)
- [x] `sci-bio-0768-sci-bio-0771-pathml`: Analyze and create new helper scripts inside its `scripts/` directory (if applicable)
- [x] `sci-bio-0769-sci-bio-0855-pydeseq2`: Check if any existing script inside the repository's `scripts/` subfolders would fit as a helper and copy it over (if applicable)
- [x] `sci-bio-0769-sci-bio-0855-pydeseq2`: Analyze and create new helper scripts inside its `scripts/` directory (if applicable)
- [x] `sci-bio-0770-sci-bio-0861-pyopenms`: Check if any existing script inside the repository's `scripts/` subfolders would fit as a helper and copy it over (if applicable)
- [x] `sci-bio-0770-sci-bio-0861-pyopenms`: Analyze and create new helper scripts inside its `scripts/` directory (if applicable)
