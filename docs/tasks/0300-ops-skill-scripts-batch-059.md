# Task 0300: Skill Scripts Batch 059

**Status**: [ ]
**Priority**: LOW
**Type**: ops

## Objective
Analyze each skill in this batch and create helper scripts inside their `scripts/` subdirectory where it makes sense, based on the `SKILL.md` instructions.

## Sub-tasks
- [ ] `web-frontend-1221-web-frontend-0997-serper`: Check if any existing script inside the repository's `scripts/` subfolders would fit as a helper and copy it over (if applicable)
- [ ] `web-frontend-1221-web-frontend-0997-serper`: Analyze and create new helper scripts inside its `scripts/` directory (if applicable)
- [ ] `web-frontend-1222-web-frontend-1027-slack`: Check if any existing script inside the repository's `scripts/` subfolders would fit as a helper and copy it over (if applicable)
- [ ] `web-frontend-1222-web-frontend-1027-slack`: Analyze and create new helper scripts inside its `scripts/` directory (if applicable)
- [ ] `web-frontend-1223-web-frontend-1036-sonoscli`: Check if any existing script inside the repository's `scripts/` subfolders would fit as a helper and copy it over (if applicable)
- [ ] `web-frontend-1223-web-frontend-1036-sonoscli`: Analyze and create new helper scripts inside its `scripts/` directory (if applicable)
- [ ] `web-frontend-1224-web-frontend-1041-spotify-player`: Check if any existing script inside the repository's `scripts/` subfolders would fit as a helper and copy it over (if applicable)
- [ ] `web-frontend-1224-web-frontend-1041-spotify-player`: Analyze and create new helper scripts inside its `scripts/` directory (if applicable)
- [ ] `web-frontend-1225-web-frontend-1090-test`: Check if any existing script inside the repository's `scripts/` subfolders would fit as a helper and copy it over (if applicable)
- [ ] `web-frontend-1225-web-frontend-1090-test`: Analyze and create new helper scripts inside its `scripts/` directory (if applicable)
- [ ] `web-frontend-1226-web-frontend-1177-verify`: Check if any existing script inside the repository's `scripts/` subfolders would fit as a helper and copy it over (if applicable)
- [ ] `web-frontend-1226-web-frontend-1177-verify`: Analyze and create new helper scripts inside its `scripts/` directory (if applicable)
- [ ] `web-frontend-1227-web-frontend-1198-weather`: Check if any existing script inside the repository's `scripts/` subfolders would fit as a helper and copy it over (if applicable)
- [ ] `web-frontend-1227-web-frontend-1198-weather`: Analyze and create new helper scripts inside its `scripts/` directory (if applicable)
- [ ] `web-frontend-1228-web-frontend-1199-weather-pollen`: Check if any existing script inside the repository's `scripts/` subfolders would fit as a helper and copy it over (if applicable)
- [ ] `web-frontend-1228-web-frontend-1199-weather-pollen`: Analyze and create new helper scripts inside its `scripts/` directory (if applicable)
- [ ] `web-frontend-1229-web-frontend-1215-whistleblower-policy-fr-malik-taiar`: Check if any existing script inside the repository's `scripts/` subfolders would fit as a helper and copy it over (if applicable)
- [ ] `web-frontend-1229-web-frontend-1215-whistleblower-policy-fr-malik-taiar`: Analyze and create new helper scripts inside its `scripts/` directory (if applicable)
- [ ] `web-frontend-1230-web-frontend-1230-x-recruiter`: Check if any existing script inside the repository's `scripts/` subfolders would fit as a helper and copy it over (if applicable)
- [ ] `web-frontend-1230-web-frontend-1230-x-recruiter`: Analyze and create new helper scripts inside its `scripts/` directory (if applicable)
- [ ] `web-frontend-1231-web-frontend-1231-x-search`: Check if any existing script inside the repository's `scripts/` subfolders would fit as a helper and copy it over (if applicable)
- [ ] `web-frontend-1231-web-frontend-1231-x-search`: Analyze and create new helper scripts inside its `scripts/` directory (if applicable)
- [ ] `web-frontend-1233-react-composition-patterns`: Check if any existing script inside the repository's `scripts/` subfolders would fit as a helper and copy it over (if applicable)
- [ ] `web-frontend-1233-react-composition-patterns`: Analyze and create new helper scripts inside its `scripts/` directory (if applicable)
- [ ] `web-frontend-1236-react-native-skills`: Check if any existing script inside the repository's `scripts/` subfolders would fit as a helper and copy it over (if applicable)
- [ ] `web-frontend-1236-react-native-skills`: Analyze and create new helper scripts inside its `scripts/` directory (if applicable)
- [ ] `web-frontend-1238-winui-app`: Check if any existing script inside the repository's `scripts/` subfolders would fit as a helper and copy it over (if applicable)
- [ ] `web-frontend-1238-winui-app`: Analyze and create new helper scripts inside its `scripts/` directory (if applicable)
