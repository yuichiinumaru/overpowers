# Task 0300: Skill Scripts Batch 020

**Status**: [x]
**Priority**: LOW
**Type**: ops

## Objective
Analyze each skill in this batch and create helper scripts inside their `scripts/` subdirectory where it makes sense, based on the `SKILL.md` instructions.

## Sub-tasks
- [x] `ai-llm-0618-ops-infra-0370-email-composer`: Check if any existing script inside the repository's `scripts/` subfolders would fit as a helper and copy it over (if applicable)
- [x] `ai-llm-0618-ops-infra-0370-email-composer`: Analyze and create new helper scripts inside its `scripts/` directory (if applicable)
- [x] `ai-llm-0682-ops-infra-0838-professional-communication`: Check if any existing script inside the repository's `scripts/` subfolders would fit as a helper and copy it over (if applicable)
- [x] `ai-llm-0682-ops-infra-0838-professional-communication`: Analyze and create new helper scripts inside its `scripts/` directory (if applicable)
- [x] `ai-llm-0695-ops-infra-0915-requesting-code-review`: Check if any existing script inside the repository's `scripts/` subfolders would fit as a helper and copy it over (if applicable)
- [x] `ai-llm-0695-ops-infra-0915-requesting-code-review`: Analyze and create new helper scripts inside its `scripts/` directory (if applicable)
- [x] `ai-llm-0733-reasoning`: Check if any existing script inside the repository's `scripts/` subfolders would fit as a helper and copy it over (if applicable)
- [x] `ai-llm-0733-reasoning`: Analyze and create new helper scripts inside its `scripts/` directory (if applicable)
- [x] `ai-llm-0906-scientific`: Check if any existing script inside the repository's `scripts/` subfolders would fit as a helper and copy it over (if applicable)
- [x] `ai-llm-0906-scientific`: Analyze and create new helper scripts inside its `scripts/` directory (if applicable)
- [x] `ai-llm-1165-ux-design-0448-frontend-design`: Check if any existing script inside the repository's `scripts/` subfolders would fit as a helper and copy it over (if applicable)
- [x] `ai-llm-1165-ux-design-0448-frontend-design`: Analyze and create new helper scripts inside its `scripts/` directory (if applicable)
- [x] `ai-llm-1172-ux-design-0517-haskell-pro`: Check if any existing script inside the repository's `scripts/` subfolders would fit as a helper and copy it over (if applicable)
- [x] `ai-llm-1172-ux-design-0517-haskell-pro`: Analyze and create new helper scripts inside its `scripts/` directory (if applicable)
- [x] `ai-llm-1203-web-frontend-0281-create-justification`: Check if any existing script inside the repository's `scripts/` subfolders would fit as a helper and copy it over (if applicable)
- [x] `ai-llm-1203-web-frontend-0281-create-justification`: Analyze and create new helper scripts inside its `scripts/` directory (if applicable)
- [x] `ai-llm-1234-build-review-interface`: Check if any existing script inside the repository's `scripts/` subfolders would fit as a helper and copy it over (if applicable)
- [x] `ai-llm-1234-build-review-interface`: Analyze and create new helper scripts inside its `scripts/` directory (if applicable)
- [x] `ai-llm-1236-claude-ai`: Check if any existing script inside the repository's `scripts/` subfolders would fit as a helper and copy it over (if applicable)
- [x] `ai-llm-1236-claude-ai`: Analyze and create new helper scripts inside its `scripts/` directory (if applicable)
- [x] `ai-llm-1237-claude-api`: Check if any existing script inside the repository's `scripts/` subfolders would fit as a helper and copy it over (if applicable)
- [x] `ai-llm-1237-claude-api`: Analyze and create new helper scripts inside its `scripts/` directory (if applicable)
- [x] `ai-llm-1238-error-analysis`: Check if any existing script inside the repository's `scripts/` subfolders would fit as a helper and copy it over (if applicable)
- [x] `ai-llm-1238-error-analysis`: Analyze and create new helper scripts inside its `scripts/` directory (if applicable)
- [x] `ai-llm-1239-eval-audit`: Check if any existing script inside the repository's `scripts/` subfolders would fit as a helper and copy it over (if applicable)
- [x] `ai-llm-1239-eval-audit`: Analyze and create new helper scripts inside its `scripts/` directory (if applicable)
- [x] `ai-llm-1240-evaluate-rag`: Check if any existing script inside the repository's `scripts/` subfolders would fit as a helper and copy it over (if applicable)
- [x] `ai-llm-1240-evaluate-rag`: Analyze and create new helper scripts inside its `scripts/` directory (if applicable)
- [x] `ai-llm-1241-generate-synthetic-data`: Check if any existing script inside the repository's `scripts/` subfolders would fit as a helper and copy it over (if applicable)
- [x] `ai-llm-1241-generate-synthetic-data`: Analyze and create new helper scripts inside its `scripts/` directory (if applicable)
- [x] `ai-llm-1242-notion-knowledge-capture`: Check if any existing script inside the repository's `scripts/` subfolders would fit as a helper and copy it over (if applicable)
- [x] `ai-llm-1242-notion-knowledge-capture`: Analyze and create new helper scripts inside its `scripts/` directory (if applicable)
- [x] `ai-llm-1243-openai-docs`: Check if any existing script inside the repository's `scripts/` subfolders would fit as a helper and copy it over (if applicable)
- [x] `ai-llm-1243-openai-docs`: Analyze and create new helper scripts inside its `scripts/` directory (if applicable)
- [x] `ai-llm-1244-stitch-loop`: Check if any existing script inside the repository's `scripts/` subfolders would fit as a helper and copy it over (if applicable)
- [x] `ai-llm-1244-stitch-loop`: Analyze and create new helper scripts inside its `scripts/` directory (if applicable)
- [x] `ai-llm-1245-validate-evaluator`: Check if any existing script inside the repository's `scripts/` subfolders would fit as a helper and copy it over (if applicable)
- [x] `ai-llm-1245-validate-evaluator`: Analyze and create new helper scripts inside its `scripts/` directory (if applicable)
- [x] `ai-llm-1246-write-judge-prompt`: Check if any existing script inside the repository's `scripts/` subfolders would fit as a helper and copy it over (if applicable)
- [x] `ai-llm-1246-write-judge-prompt`: Analyze and create new helper scripts inside its `scripts/` directory (if applicable)
