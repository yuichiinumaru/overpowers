# Task 0300: Skill Scripts Batch 042

**Status**: [ ]
**Priority**: LOW
**Type**: ops

## Objective
Analyze each skill in this batch and create helper scripts inside their `scripts/` subdirectory where it makes sense, based on the `SKILL.md` instructions.

## Sub-tasks
- [ ] `sci-quant-0855-sci-quant-0616-literature-review`: Check if any existing script inside the repository's `scripts/` subfolders would fit as a helper and copy it over (if applicable)
- [ ] `sci-quant-0855-sci-quant-0616-literature-review`: Analyze and create new helper scripts inside its `scripts/` directory (if applicable)
- [ ] `sci-quant-0856-sci-quant-0642-math-model-selector`: Check if any existing script inside the repository's `scripts/` subfolders would fit as a helper and copy it over (if applicable)
- [ ] `sci-quant-0856-sci-quant-0642-math-model-selector`: Analyze and create new helper scripts inside its `scripts/` directory (if applicable)
- [ ] `sci-quant-0858-sci-quant-0682-molfeat`: Check if any existing script inside the repository's `scripts/` subfolders would fit as a helper and copy it over (if applicable)
- [ ] `sci-quant-0858-sci-quant-0682-molfeat`: Analyze and create new helper scripts inside its `scripts/` directory (if applicable)
- [ ] `sci-quant-0861-sci-quant-0743-openalex-database`: Check if any existing script inside the repository's `scripts/` subfolders would fit as a helper and copy it over (if applicable)
- [ ] `sci-quant-0861-sci-quant-0743-openalex-database`: Analyze and create new helper scripts inside its `scripts/` directory (if applicable)
- [ ] `sci-quant-0862-sci-quant-0745-openclaw-search`: Check if any existing script inside the repository's `scripts/` subfolders would fit as a helper and copy it over (if applicable)
- [ ] `sci-quant-0862-sci-quant-0745-openclaw-search`: Analyze and create new helper scripts inside its `scripts/` directory (if applicable)
- [ ] `sci-quant-0863-sci-quant-0859-pymatgen`: Check if any existing script inside the repository's `scripts/` subfolders would fit as a helper and copy it over (if applicable)
- [ ] `sci-quant-0863-sci-quant-0859-pymatgen`: Analyze and create new helper scripts inside its `scripts/` directory (if applicable)
- [ ] `sci-quant-0864-sci-quant-0864-pytdc`: Check if any existing script inside the repository's `scripts/` subfolders would fit as a helper and copy it over (if applicable)
- [ ] `sci-quant-0864-sci-quant-0864-pytdc`: Analyze and create new helper scripts inside its `scripts/` directory (if applicable)
- [ ] `sci-quant-0865-sci-quant-0870-qiskit`: Check if any existing script inside the repository's `scripts/` subfolders would fit as a helper and copy it over (if applicable)
- [ ] `sci-quant-0865-sci-quant-0870-qiskit`: Analyze and create new helper scripts inside its `scripts/` directory (if applicable)
- [ ] `sci-quant-0866-sci-quant-0881-qutip`: Check if any existing script inside the repository's `scripts/` subfolders would fit as a helper and copy it over (if applicable)
- [ ] `sci-quant-0866-sci-quant-0881-qutip`: Analyze and create new helper scripts inside its `scripts/` directory (if applicable)
- [ ] `sci-quant-0867-sci-quant-0950-scientific-brainstorming`: Check if any existing script inside the repository's `scripts/` subfolders would fit as a helper and copy it over (if applicable)
- [ ] `sci-quant-0867-sci-quant-0950-scientific-brainstorming`: Analyze and create new helper scripts inside its `scripts/` directory (if applicable)
- [ ] `sci-quant-0868-sci-quant-0954-scientific-slides`: Check if any existing script inside the repository's `scripts/` subfolders would fit as a helper and copy it over (if applicable)
- [ ] `sci-quant-0868-sci-quant-0954-scientific-slides`: Analyze and create new helper scripts inside its `scripts/` directory (if applicable)
- [ ] `sci-quant-0869-sci-quant-1077-tavily-best-practices`: Check if any existing script inside the repository's `scripts/` subfolders would fit as a helper and copy it over (if applicable)
- [ ] `sci-quant-0869-sci-quant-1077-tavily-best-practices`: Analyze and create new helper scripts inside its `scripts/` directory (if applicable)
- [ ] `sci-quant-0870-sci-quant-1078-tavily-search-pro`: Check if any existing script inside the repository's `scripts/` subfolders would fit as a helper and copy it over (if applicable)
- [ ] `sci-quant-0870-sci-quant-1078-tavily-search-pro`: Analyze and create new helper scripts inside its `scripts/` directory (if applicable)
- [ ] `sci-quant-0871-sci-quant-1098-threejs-animation`: Check if any existing script inside the repository's `scripts/` subfolders would fit as a helper and copy it over (if applicable)
- [ ] `sci-quant-0871-sci-quant-1098-threejs-animation`: Analyze and create new helper scripts inside its `scripts/` directory (if applicable)
- [ ] `sci-quant-0873-sci-quant-1132-torchdrug`: Check if any existing script inside the repository's `scripts/` subfolders would fit as a helper and copy it over (if applicable)
- [ ] `sci-quant-0873-sci-quant-1132-torchdrug`: Analyze and create new helper scripts inside its `scripts/` directory (if applicable)
- [ ] `sci-quant-0874-sci-quant-1136-transitions`: Check if any existing script inside the repository's `scripts/` subfolders would fit as a helper and copy it over (if applicable)
- [ ] `sci-quant-0874-sci-quant-1136-transitions`: Analyze and create new helper scripts inside its `scripts/` directory (if applicable)
- [ ] `sci-sci-0875-sci-sci-0007-academic-researcher`: Check if any existing script inside the repository's `scripts/` subfolders would fit as a helper and copy it over (if applicable)
- [ ] `sci-sci-0875-sci-sci-0007-academic-researcher`: Analyze and create new helper scripts inside its `scripts/` directory (if applicable)
- [ ] `sci-sci-0876-sci-sci-0111-backtesting-frameworks`: Check if any existing script inside the repository's `scripts/` subfolders would fit as a helper and copy it over (if applicable)
- [ ] `sci-sci-0876-sci-sci-0111-backtesting-frameworks`: Analyze and create new helper scripts inside its `scripts/` directory (if applicable)
- [ ] `sci-sci-0877-sci-sci-0124-baoyu-infographic`: Check if any existing script inside the repository's `scripts/` subfolders would fit as a helper and copy it over (if applicable)
- [ ] `sci-sci-0877-sci-sci-0124-baoyu-infographic`: Analyze and create new helper scripts inside its `scripts/` directory (if applicable)
- [ ] `sci-sci-0878-sci-sci-0287-creating-financial-models`: Check if any existing script inside the repository's `scripts/` subfolders would fit as a helper and copy it over (if applicable)
- [ ] `sci-sci-0878-sci-sci-0287-creating-financial-models`: Analyze and create new helper scripts inside its `scripts/` directory (if applicable)
