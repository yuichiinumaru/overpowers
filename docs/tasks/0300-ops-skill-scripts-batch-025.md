# Task 0300: Skill Scripts Batch 025

**Status**: [x]
**Priority**: LOW
**Type**: ops

## Objective
Analyze each skill in this batch and create helper scripts inside their `scripts/` subdirectory where it makes sense, based on the `SKILL.md` instructions.

## Sub-tasks
+ [x] `media-content-0503-media-content-0003-3d`: Check if any existing script inside the repository's `scripts/` subfolders would fit as a helper and copy it over (if applicable)
+ [x] `media-content-0503-media-content-0003-3d`: Analyze and create new helper scripts inside its `scripts/` directory (if applicable)
+ [x] `media-content-0504-media-content-0008-accessibility`: Check if any existing script inside the repository's `scripts/` subfolders would fit as a helper and copy it over (if applicable)
+ [x] `media-content-0504-media-content-0008-accessibility`: Analyze and create new helper scripts inside its `scripts/` directory (if applicable)
+ [x] `media-content-0505-media-content-0009-accessibility-auditor`: Check if any existing script inside the repository's `scripts/` subfolders would fit as a helper and copy it over (if applicable)
+ [x] `media-content-0505-media-content-0009-accessibility-auditor`: Analyze and create new helper scripts inside its `scripts/` directory (if applicable)
+ [x] `media-content-0506-media-content-0010-accessibility-compliance`: Check if any existing script inside the repository's `scripts/` subfolders would fit as a helper and copy it over (if applicable)
+ [x] `media-content-0506-media-content-0010-accessibility-compliance`: Analyze and create new helper scripts inside its `scripts/` directory (if applicable)
+ [x] `media-content-0507-media-content-0063-animations`: Check if any existing script inside the repository's `scripts/` subfolders would fit as a helper and copy it over (if applicable)
+ [x] `media-content-0507-media-content-0063-animations`: Analyze and create new helper scripts inside its `scripts/` directory (if applicable)
+ [x] `media-content-0508-media-content-0081-assets`: Check if any existing script inside the repository's `scripts/` subfolders would fit as a helper and copy it over (if applicable)
+ [x] `media-content-0508-media-content-0081-assets`: Analyze and create new helper scripts inside its `scripts/` directory (if applicable)
+ [x] `media-content-0509-media-content-0086-audio`: Check if any existing script inside the repository's `scripts/` subfolders would fit as a helper and copy it over (if applicable)
+ [x] `media-content-0509-media-content-0086-audio`: Analyze and create new helper scripts inside its `scripts/` directory (if applicable)
+ [x] `media-content-0510-media-content-0118-baoyu-compress-image`: Check if any existing script inside the repository's `scripts/` subfolders would fit as a helper and copy it over (if applicable)
+ [x] `media-content-0510-media-content-0118-baoyu-compress-image`: Analyze and create new helper scripts inside its `scripts/` directory (if applicable)
+ [x] `media-content-0511-media-content-0143-birdnet`: Check if any existing script inside the repository's `scripts/` subfolders would fit as a helper and copy it over (if applicable)
+ [x] `media-content-0511-media-content-0143-birdnet`: Analyze and create new helper scripts inside its `scripts/` directory (if applicable)
+ [x] `media-content-0512-media-content-0183-calculate-metadata`: Check if any existing script inside the repository's `scripts/` subfolders would fit as a helper and copy it over (if applicable)
+ [x] `media-content-0512-media-content-0183-calculate-metadata`: Analyze and create new helper scripts inside its `scripts/` directory (if applicable)
+ [x] `media-content-0513-media-content-0186-camsnap`: Check if any existing script inside the repository's `scripts/` subfolders would fit as a helper and copy it over (if applicable)
+ [x] `media-content-0513-media-content-0186-camsnap`: Analyze and create new helper scripts inside its `scripts/` directory (if applicable)
+ [x] `media-content-0514-media-content-0187-can-decode`: Check if any existing script inside the repository's `scripts/` subfolders would fit as a helper and copy it over (if applicable)
+ [x] `media-content-0514-media-content-0187-can-decode`: Analyze and create new helper scripts inside its `scripts/` directory (if applicable)
+ [x] `media-content-0515-media-content-0190-canvas-design`: Check if any existing script inside the repository's `scripts/` subfolders would fit as a helper and copy it over (if applicable)
+ [x] `media-content-0515-media-content-0190-canvas-design`: Analyze and create new helper scripts inside its `scripts/` directory (if applicable)
+ [x] `media-content-0516-media-content-0198-charts`: Check if any existing script inside the repository's `scripts/` subfolders would fit as a helper and copy it over (if applicable)
+ [x] `media-content-0516-media-content-0198-charts`: Analyze and create new helper scripts inside its `scripts/` directory (if applicable)
+ [x] `media-content-0517-media-content-0250-compositions`: Check if any existing script inside the repository's `scripts/` subfolders would fit as a helper and copy it over (if applicable)
+ [x] `media-content-0517-media-content-0250-compositions`: Analyze and create new helper scripts inside its `scripts/` directory (if applicable)
+ [x] `media-content-0518-media-content-0258-content-creation`: Check if any existing script inside the repository's `scripts/` subfolders would fit as a helper and copy it over (if applicable)
+ [x] `media-content-0518-media-content-0258-content-creation`: Analyze and create new helper scripts inside its `scripts/` directory (if applicable)
+ [x] `media-content-0519-media-content-0262-content-shipped`: Check if any existing script inside the repository's `scripts/` subfolders would fit as a helper and copy it over (if applicable)
+ [x] `media-content-0519-media-content-0262-content-shipped`: Analyze and create new helper scripts inside its `scripts/` directory (if applicable)
+ [x] `media-content-0520-media-content-0279-create-blog-post`: Check if any existing script inside the repository's `scripts/` subfolders would fit as a helper and copy it over (if applicable)
+ [x] `media-content-0520-media-content-0279-create-blog-post`: Analyze and create new helper scripts inside its `scripts/` directory (if applicable)
+ [x] `media-content-0521-media-content-0329-design-md`: Check if any existing script inside the repository's `scripts/` subfolders would fit as a helper and copy it over (if applicable)
+ [x] `media-content-0521-media-content-0329-design-md`: Analyze and create new helper scripts inside its `scripts/` directory (if applicable)
+ [x] `media-content-0522-media-content-0331-design-system-starter`: Check if any existing script inside the repository's `scripts/` subfolders would fit as a helper and copy it over (if applicable)
+ [x] `media-content-0522-media-content-0331-design-system-starter`: Analyze and create new helper scripts inside its `scripts/` directory (if applicable)
