# Task 0300: Skill Scripts Batch 039

**Status**: [ ]
**Priority**: LOW
**Type**: ops

## Objective
Analyze each skill in this batch and create helper scripts inside their `scripts/` subdirectory where it makes sense, based on the `SKILL.md` instructions.

## Sub-tasks
- [ ] `sci-bio-0791-sci-bio-1080-tcga-bulk-data-preprocessing-with-omicverse`: Check if any existing script inside the repository's `scripts/` subfolders would fit as a helper and copy it over (if applicable)
- [ ] `sci-bio-0791-sci-bio-1080-tcga-bulk-data-preprocessing-with-omicverse`: Analyze and create new helper scripts inside its `scripts/` directory (if applicable)
- [ ] `sci-bio-0792-sci-bio-1120-tooluniverse-expression-data-retrieval`: Check if any existing script inside the repository's `scripts/` subfolders would fit as a helper and copy it over (if applicable)
- [ ] `sci-bio-0792-sci-bio-1120-tooluniverse-expression-data-retrieval`: Analyze and create new helper scripts inside its `scripts/` directory (if applicable)
- [ ] `sci-bio-0793-sci-bio-1126-tooluniverse-protein-therapeutic-design`: Check if any existing script inside the repository's `scripts/` subfolders would fit as a helper and copy it over (if applicable)
- [ ] `sci-bio-0793-sci-bio-1126-tooluniverse-protein-therapeutic-design`: Analyze and create new helper scripts inside its `scripts/` directory (if applicable)
- [ ] `sci-bio-0794-sci-bio-1127-tooluniverse-rare-disease-diagnosis`: Check if any existing script inside the repository's `scripts/` subfolders would fit as a helper and copy it over (if applicable)
- [ ] `sci-bio-0794-sci-bio-1127-tooluniverse-rare-disease-diagnosis`: Analyze and create new helper scripts inside its `scripts/` directory (if applicable)
- [ ] `sci-bio-0795-sci-bio-1129-tooluniverse-sequence-retrieval`: Check if any existing script inside the repository's `scripts/` subfolders would fit as a helper and copy it over (if applicable)
- [ ] `sci-bio-0795-sci-bio-1129-tooluniverse-sequence-retrieval`: Analyze and create new helper scripts inside its `scripts/` directory (if applicable)
- [ ] `sci-bio-0796-sci-bio-1152-uniprot-database`: Check if any existing script inside the repository's `scripts/` subfolders would fit as a helper and copy it over (if applicable)
- [ ] `sci-bio-0796-sci-bio-1152-uniprot-database`: Analyze and create new helper scripts inside its `scripts/` directory (if applicable)
- [ ] `sci-bio-0797-sci-bio-1172-venue-templates`: Check if any existing script inside the repository's `scripts/` subfolders would fit as a helper and copy it over (if applicable)
- [ ] `sci-bio-0797-sci-bio-1172-venue-templates`: Analyze and create new helper scripts inside its `scripts/` directory (if applicable)
- [ ] `sci-chem-0798-sci-chem-0128-baoyu-slide-deck`: Check if any existing script inside the repository's `scripts/` subfolders would fit as a helper and copy it over (if applicable)
- [ ] `sci-chem-0798-sci-chem-0128-baoyu-slide-deck`: Analyze and create new helper scripts inside its `scripts/` directory (if applicable)
- [ ] `sci-chem-0799-sci-chem-0134-beads`: Check if any existing script inside the repository's `scripts/` subfolders would fit as a helper and copy it over (if applicable)
- [ ] `sci-chem-0799-sci-chem-0134-beads`: Analyze and create new helper scripts inside its `scripts/` directory (if applicable)
- [ ] `sci-chem-0800-sci-chem-0200-chembl-database`: Check if any existing script inside the repository's `scripts/` subfolders would fit as a helper and copy it over (if applicable)
- [ ] `sci-chem-0800-sci-chem-0200-chembl-database`: Analyze and create new helper scripts inside its `scripts/` directory (if applicable)
- [ ] `sci-chem-0801-sci-chem-0215-clinical-decision-support`: Check if any existing script inside the repository's `scripts/` subfolders would fit as a helper and copy it over (if applicable)
- [ ] `sci-chem-0801-sci-chem-0215-clinical-decision-support`: Analyze and create new helper scripts inside its `scripts/` directory (if applicable)
- [ ] `sci-chem-0802-sci-chem-0217-clinicaltrials-database`: Check if any existing script inside the repository's `scripts/` subfolders would fit as a helper and copy it over (if applicable)
- [ ] `sci-chem-0802-sci-chem-0217-clinicaltrials-database`: Analyze and create new helper scripts inside its `scripts/` directory (if applicable)
- [ ] `sci-chem-0803-sci-chem-0218-clinpgx-database`: Check if any existing script inside the repository's `scripts/` subfolders would fit as a helper and copy it over (if applicable)
- [ ] `sci-chem-0803-sci-chem-0218-clinpgx-database`: Analyze and create new helper scripts inside its `scripts/` directory (if applicable)
- [ ] `sci-chem-0804-sci-chem-0275-cosmic-database`: Check if any existing script inside the repository's `scripts/` subfolders would fit as a helper and copy it over (if applicable)
- [ ] `sci-chem-0804-sci-chem-0275-cosmic-database`: Analyze and create new helper scripts inside its `scripts/` directory (if applicable)
- [ ] `sci-chem-0805-sci-chem-0317-datamol`: Check if any existing script inside the repository's `scripts/` subfolders would fit as a helper and copy it over (if applicable)
- [ ] `sci-chem-0805-sci-chem-0317-datamol`: Analyze and create new helper scripts inside its `scripts/` directory (if applicable)
- [ ] `sci-chem-0806-sci-chem-0334-diffdock`: Check if any existing script inside the repository's `scripts/` subfolders would fit as a helper and copy it over (if applicable)
- [ ] `sci-chem-0806-sci-chem-0334-diffdock`: Analyze and create new helper scripts inside its `scripts/` directory (if applicable)
- [ ] `sci-chem-0807-sci-chem-0362-drug-repurposing`: Check if any existing script inside the repository's `scripts/` subfolders would fit as a helper and copy it over (if applicable)
- [ ] `sci-chem-0807-sci-chem-0362-drug-repurposing`: Analyze and create new helper scripts inside its `scripts/` directory (if applicable)
- [ ] `sci-chem-0808-sci-chem-0363-drugbank-database`: Check if any existing script inside the repository's `scripts/` subfolders would fit as a helper and copy it over (if applicable)
- [ ] `sci-chem-0808-sci-chem-0363-drugbank-database`: Analyze and create new helper scripts inside its `scripts/` directory (if applicable)
- [ ] `sci-chem-0809-sci-chem-0380-esm`: Check if any existing script inside the repository's `scripts/` subfolders would fit as a helper and copy it over (if applicable)
- [ ] `sci-chem-0809-sci-chem-0380-esm`: Analyze and create new helper scripts inside its `scripts/` directory (if applicable)
- [ ] `sci-chem-0810-sci-chem-0394-exploratory-data-analysis`: Check if any existing script inside the repository's `scripts/` subfolders would fit as a helper and copy it over (if applicable)
- [ ] `sci-chem-0810-sci-chem-0394-exploratory-data-analysis`: Analyze and create new helper scripts inside its `scripts/` directory (if applicable)
