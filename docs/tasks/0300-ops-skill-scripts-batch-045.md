# Task 0300: Skill Scripts Batch 045

**Status**: [x]
**Priority**: LOW
**Type**: ops

## Objective
Analyze each skill in this batch and create helper scripts inside their `scripts/` subdirectory where it makes sense, based on the `SKILL.md` instructions.

## Sub-tasks
- [x] `sec-safety-0925-sec-safety-0084-attack-tree-construction`: Check if any existing script inside the repository's `scripts/` subfolders would fit as a helper and copy it over (if applicable)
- [x] `sec-safety-0925-sec-safety-0084-attack-tree-construction`: Analyze and create new helper scripts inside its `scripts/` directory (if applicable)
- [x] `sec-safety-0926-sec-safety-0085-aubrai-longevity`: Check if any existing script inside the repository's `scripts/` subfolders would fit as a helper and copy it over (if applicable)
- [x] `sec-safety-0926-sec-safety-0085-aubrai-longevity`: Analyze and create new helper scripts inside its `scripts/` directory (if applicable)
- [x] `sec-safety-0927-sec-safety-0089-audit-context-building`: Check if any existing script inside the repository's `scripts/` subfolders would fit as a helper and copy it over (if applicable)
- [x] `sec-safety-0927-sec-safety-0089-audit-context-building`: Analyze and create new helper scripts inside its `scripts/` directory (if applicable)
- [x] `sec-safety-0928-sec-safety-0090-audit-prep-assistant`: Check if any existing script inside the repository's `scripts/` subfolders would fit as a helper and copy it over (if applicable)
- [x] `sec-safety-0928-sec-safety-0090-audit-prep-assistant`: Analyze and create new helper scripts inside its `scripts/` directory (if applicable)
- [x] `sec-safety-0929-sec-safety-0093-authentication`: Check if any existing script inside the repository's `scripts/` subfolders would fit as a helper and copy it over (if applicable)
- [x] `sec-safety-0929-sec-safety-0093-authentication`: Analyze and create new helper scripts inside its `scripts/` directory (if applicable)
- [x] `sec-safety-0932-sec-safety-0096-aws-cost-operations`: Check if any existing script inside the repository's `scripts/` subfolders would fit as a helper and copy it over (if applicable)
- [x] `sec-safety-0932-sec-safety-0096-aws-cost-operations`: Analyze and create new helper scripts inside its `scripts/` directory (if applicable)
- [x] `sec-safety-0933-sec-safety-0098-aws-penetration-testing`: Check if any existing script inside the repository's `scripts/` subfolders would fit as a helper and copy it over (if applicable)
- [x] `sec-safety-0933-sec-safety-0098-aws-penetration-testing`: Analyze and create new helper scripts inside its `scripts/` directory (if applicable)
- [x] `sec-safety-0934-sec-safety-0099-aws-serverless-eda`: Check if any existing script inside the repository's `scripts/` subfolders would fit as a helper and copy it over (if applicable)
- [x] `sec-safety-0934-sec-safety-0099-aws-serverless-eda`: Analyze and create new helper scripts inside its `scripts/` directory (if applicable)
- [x] `sec-safety-0935-sec-safety-0108-azure-devops-cli`: Check if any existing script inside the repository's `scripts/` subfolders would fit as a helper and copy it over (if applicable)
- [x] `sec-safety-0935-sec-safety-0108-azure-devops-cli`: Analyze and create new helper scripts inside its `scripts/` directory (if applicable)
- [x] `sec-safety-0936-sec-safety-0116-bamboohr-automation`: Check if any existing script inside the repository's `scripts/` subfolders would fit as a helper and copy it over (if applicable)
- [x] `sec-safety-0936-sec-safety-0116-bamboohr-automation`: Analyze and create new helper scripts inside its `scripts/` directory (if applicable)
- [x] `sec-safety-0937-sec-safety-0137-benchling-integration`: Check if any existing script inside the repository's `scripts/` subfolders would fit as a helper and copy it over (if applicable)
- [x] `sec-safety-0937-sec-safety-0137-benchling-integration`: Analyze and create new helper scripts inside its `scripts/` directory (if applicable)
- [x] `sec-safety-0938-sec-safety-0139-biomni`: Check if any existing script inside the repository's `scripts/` subfolders would fit as a helper and copy it over (if applicable)
- [x] `sec-safety-0938-sec-safety-0139-biomni`: Analyze and create new helper scripts inside its `scripts/` directory (if applicable)
- [x] `sec-safety-0939-sec-safety-0145-blockchain-web3`: Check if any existing script inside the repository's `scripts/` subfolders would fit as a helper and copy it over (if applicable)
- [x] `sec-safety-0939-sec-safety-0145-blockchain-web3`: Analyze and create new helper scripts inside its `scripts/` directory (if applicable)
- [x] `sec-safety-0940-sec-safety-0158-brand-voice-consistency`: Check if any existing script inside the repository's `scripts/` subfolders would fit as a helper and copy it over (if applicable)
- [x] `sec-safety-0940-sec-safety-0158-brand-voice-consistency`: Analyze and create new helper scripts inside its `scripts/` directory (if applicable)
- [x] `sec-safety-0941-sec-safety-0162-brightdata-web-mcp`: Check if any existing script inside the repository's `scripts/` subfolders would fit as a helper and copy it over (if applicable)
- [x] `sec-safety-0941-sec-safety-0162-brightdata-web-mcp`: Analyze and create new helper scripts inside its `scripts/` directory (if applicable)
- [x] `sec-safety-0942-sec-safety-0163-broken-authentication`: Check if any existing script inside the repository's `scripts/` subfolders would fit as a helper and copy it over (if applicable)
- [x] `sec-safety-0942-sec-safety-0163-broken-authentication`: Analyze and create new helper scripts inside its `scripts/` directory (if applicable)
- [x] `sec-safety-0943-sec-safety-0174-burp-suite-testing`: Check if any existing script inside the repository's `scripts/` subfolders would fit as a helper and copy it over (if applicable)
- [x] `sec-safety-0943-sec-safety-0174-burp-suite-testing`: Analyze and create new helper scripts inside its `scripts/` directory (if applicable)
- [x] `sec-safety-0944-sec-safety-0177-c4-architecture`: Check if any existing script inside the repository's `scripts/` subfolders would fit as a helper and copy it over (if applicable)
- [x] `sec-safety-0944-sec-safety-0177-c4-architecture`: Analyze and create new helper scripts inside its `scripts/` directory (if applicable)
- [x] `sec-safety-0945-sec-safety-0181-cal-com-automation`: Check if any existing script inside the repository's `scripts/` subfolders would fit as a helper and copy it over (if applicable)
- [x] `sec-safety-0945-sec-safety-0181-cal-com-automation`: Analyze and create new helper scripts inside its `scripts/` directory (if applicable)
- [x] `sec-safety-0946-sec-safety-0182-calcom-api`: Check if any existing script inside the repository's `scripts/` subfolders would fit as a helper and copy it over (if applicable)
- [x] `sec-safety-0946-sec-safety-0182-calcom-api`: Analyze and create new helper scripts inside its `scripts/` directory (if applicable)
