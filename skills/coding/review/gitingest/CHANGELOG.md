# Changelog for ai-llm-gitingest skill

## [2026-03-15] - Initial release
### Added
- Created the `ai-llm-gitingest` skill to convert GitHub repositories into AI-friendly Markdown digests.
- Added support for CLI integration via scripts.
- Implemented `ingest_manager.py` for handling ingestion logic.
- Added `run.py` as the main entry point.
- Created `SKILL.md` and `README.md` for documentation.
- Added `requirements.txt` with `gitingest` dependency.
